# Decyzje — Pudełko — sen, alarm, NVS, WiFi

firmware/PillBox.ino: wybudzenia, alarm, pamięć trwała, sieci

Indeks wszystkich decyzji: `DECYZJE.md`. Tu są **pełne** wpisy z tego obszaru (20 szt.), od najnowszego.

| # | Decyzja | Co by było bez tego |
|---|---|---|
| **D70** | **Backoff nie działał w życiu, choć miał test — i zjadał baterię: „meldunek bez sieci" co 17 minut przez cały dzień** (1.45.3) | Zgłoszenie Kuby to zrzut historii pudełka: wpisy `meldunek bez sieci` o **9:33, 9:50, 10:07, 10:24 … 17:46** — równe **17 minut**, bez przerwy, przez osiem godzin. Bateria w tym czasie z **73% na 50%**. W tym tempie ogniwo pada w niecałe dwa dni.<br>**Zdążyłem mu wcześniej powiedzieć, że jest odwrotnie** — że odstępy rosną 15 → 30 → 60 → 120 → 240 minut. Tak mówił kod `planNextSleep()` i tak mówił test. Log mówił co innego i to log miał rację.<br>**Przyczyna: `rtcRetryCount` rósł w JEDNYM miejscu — w `reportEvent()`**, czyli wyłącznie na ścieżce otwarcia wieczka. Wybudzenie z timera, które nie złapało sieci, kończyło się samym `rtcStatusDirty = true`; licznika nikt nie podnosił, więc przesunięcie zostawało na zerze i `RETRY_BASE_S << 0` dawało 15 minut. W kółko. Te obserwowane 17 minut to 15 minut snu plus około dwóch minut czuwania na próby połączenia.<br>**Test istniał i przechodził — sprawdzał nie to.** `rtcRetryCount = 3` → 7200 s: matematyka backoffu była poprawna. Nikt nie sprawdził, czy ten licznik **ma jak urosnąć**. To najdroższa odmiana fałszywego pokrycia: zielony test przy zepsutym zachowaniu, bo mierzy mechanizm w oderwaniu od okablowania.<br>**Naprawa: decyzja przeniesiona do `goToSleep()`** — jedynego miejsca, przez które przechodzi **każda** ścieżka wybudzenia. Zaległość w kolejce albo niedostarczony status znaczą „nie dowiozłem" i rozrzedzają następną próbę; czysto zakończone wybudzenie zeruje przesunięcie od razu. Podnoszenie w `reportEvent()` **usunięte** — w obu miejscach naraz liczyłoby jedno wybudzenie dwa razy.<br>**Żeby dało się to przetestować, sama decyzja wyszła do czystej funkcji** `kolejnePrzesuniecie()`. `goToSleep()` dotyka snu i sprzętu, więc w testach nie ruszy — a to właśnie tam siedział błąd. Nowy test idzie przez **kolejne** wybudzenia i sprawdza cały ciąg odstępów aż do sufitu 4 h, zamiast jednego przypadku z podstawioną liczbą.<br>**Audyt pilnuje teraz dwóch rzeczy naraz:** że rozrzedzanie stoi w `goToSleep()`, i że **nigdzie indziej** licznik nie rośnie. Sprawdzone mutacją — powrót do starego `rtcRetryCount++` wywala obie kontrole.<br>**Kontekst, który to spotęgował:** pudełko chodziło **bez wpiętej anteny** (XIAO ESP32-C3 nie ma żadnej wbudowanej), więc porażki były regułą, a nie wyjątkiem. Backoff powstał przy założeniu, że są rzadkie — i przy tym założeniu nikt nie zauważyłby, że nie działa. Dwie usterki, z których każda osobno byłaby ledwo widoczna, razem dawały pudełko padające w dwa dni. |
| **D68** | **Przytrzymanie przycisku gubiło się w czasie, gdy pudełko gadało z siecią** — portal WiFi wstawał „po którejś próbie" (1.44.1) | Zgłoszenie Kuby: *„otwieram to wieczko i przytrzymuję przycisk, i nic nie wyszukuje mi"*, a chwilę później: *„po którejś próbie mi się udało, ale tak nie powinno być"*. Miał rację co do jednego i drugiego — gest działał, tylko wymagał trzymania kilkanaście sekund zamiast 1,8 s, i nikt o tym nie wiedział.<br>**Przyczyna jest w kolejności, nie w przycisku.** Przycisk fizycznie nie budzi układu (maska wybudzania ma jeden wspólny poziom, a kontaktron i przycisk potrzebują przeciwnych), więc jest obserwowany **wyłącznie** w `czekajNaZamkniecieIGest()`. A ta funkcja startuje dopiero **po** `reportEvent()`: po połączeniu z siecią, zalogowaniu do bazy, wysłaniu zdarzenia i statusu. To trwa od dwóch do kilkunastu sekund — i przez cały ten czas przycisk jest w próżni. Gdy pętla wreszcie ruszała, odliczała swoje 1,8 s **od nowa**, więc łączny czas trzymania musiał przekroczyć całą robotę z radiem. Puszczenie przycisku wcześniej gubiło gest bez śladu.<br>**Naprawa jest jedna i tania: próbkujemy przycisk NAJWCZEŚNIEJ, jak się da** — tuż po `configureInputs()`, zanim cokolwiek dotknie radia (`msPrzyciskOd`). Jeżeli przy wejściu do pętli przycisk **nadal** jest wciśnięty, a widzieliśmy go takim na starcie wybudzenia, to znaczy, że człowiek trzyma go od tamtej chwili — odliczamy więc od niej. Jeden odczyt pinu, zero kosztu.<br>**Druga połowa naprawy to dźwięk.** Kliknięcie miało swoje piknięcie od dawna, **przytrzymanie nie miało żadnego** — więc do chwili, gdy wstanie portal (trzy piknięcia), nie było wiadomo, czy pudełko w ogóle zauważyło gest. Teraz przyjęcie przytrzymania potwierdza się od razu, w momencie przekroczenia progu. Ta sama zasada co przy aktualizacji: **człowiek stojący nad pudełkiem musi wiedzieć, że jego gest został przyjęty**, zanim zacznie się to, co trwa.<br>**Rodzina błędu jest znajoma** — to trzeci raz w tym projekcie, gdy funkcja czeka na coś, co dawno minęło, bo obserwator ruszył za późno: D61 (zlecenie OTA złożone między odczytem konfiguracji a próbą), D62 (powtórne otwarcie wieczka nie czytało ustawień w ogóle) i teraz gest. Wspólny morał: **sprawdź, KIEDY zaczynasz patrzeć, a nie tylko czy patrzysz.**<br>Audyt: trzy nowe kontrole — próbka przed robotą z siecią, odliczanie od chwili wciśnięcia, dźwiękowe potwierdzenie. Sprawdzone mutacją w obie strony. |
| **D56** | Znacznik „alarm już odzwoniony" jest **maską slotów**, nie flagą na całą dobę; „missed" leci dopiero po **ostatnim** przypomnieniu doby (znalezione przy pytaniu Kuby „czy jest coś ważnego, o czym nie pomyślałem") | Naprawa **D23** (zgłoszenie z wyjazdu: *„pika już 3 raz od 18:30"*) postawiła znacznik na **całą dobę**. Skutek uboczny, którego wtedy nikt nie zobaczył: pierwsze przypomnienie, poddając się po `MAX_ALARM_RETRIES`, wyciszało **wszystkie następne**. Druga pozycja w harmonogramie znaczy „przypomnij jeszcze raz o 23:00" (CLAUDE.md 4b) i milczała **dokładnie wtedy, kiedy jest potrzebna** — gdy pierwsze przypomnienie przeszło bez odzewu. Kuba zgłosił potem, że chce móc ustawić ich nawet dziesięć; bez tej poprawki dziewięć z nich byłoby ozdobą.<br>`rtcAlarmDoneMask` (16 bitów, slot = bit) zamiast pojedynczej flagi. **D23 zostaje co do joty**: ten sam slot nie wraca w swoim oknie ±90 min. Nowa doba kasuje **całą** maskę, nie tylko bieżący bit. Bez zegara zostaje stara ochrona czasowa — bez zegara `matchSlot()` i tak zwraca −1, więc rozróżnianie slotów byłoby zgadywaniem, a ciszej znaczy tu bezpieczniej.<br>**Drugi błąd w tym samym miejscu: „missed" leciał po PIERWSZYM przypomnieniu.** Dzień robił się czerwony o 20:00, choć o 23:00 pudełko miało jeszcze raz zapytać — a gdyby tabletka poszła o 22:00, aplikacja musiałaby ten wpis odkręcać. To rodzina **B27/B29**: wpisy „missed" ścigające się z prawdziwą dawką. Teraz zgłasza je dopiero slot, po którym w tej dobie nie ma już nic; doby kończące się bez dawki i tak domyka `checkDayRollover()`.<br>**Ostatnia szansa liczona w ramie doby lekowej, nie na zegarze:** przy godzinach `["23:00","07:30"]` ostatnia jest 23:00, bo 07:30 należy już do następnej doby. Test sprawdza obie kolejności zapisu.<br>Maska przeżywa reset (NVS `almMask`) i jest odtwarzana **razem z dniem, nigdy osobno** — dzień bez maski znaczyłby „coś już odzwoniono, ale nie wiadomo co".<br>**Sprawdzone mutacją:** powrót do flagi na całą dobę wywala kontrolę „drugie przypomnienie NIE jest wyciszone przez pierwsze". Audyt pilnuje dodatkowo **kolejności** — warunek `slot == ostatniSlotDoby()` musi stać PRZED zapisem „missed", inaczej wisiałby obok jako ozdoba.<br>Aplikacja dostała **sufit 12 przypomnień wzięty z firmware** (`String slots[12]`, `parseSchedule()` po cichu urywa resztę). Bez tego trzynaste przypomnienie pokazałoby się na ekranie i poleciało do bazy, a pudełko nigdy by o nim nie usłyszało — cicha rozbieżność ekranu z urządzeniem, rodzina B9/B10. |
| **D47** | Historia nieudanych zapisów NVS trafia teraz **do bazy**, nie tylko do RAM (prośba Kuby: *„może w bazie damy jakiś zapis, żeby te nieudane tam wrzucał"*, hipoteza: *„albo to po alarmie się pojawiło"*) | D46 dało tylko OSTATNI nieudany klucz — wystarczyło do klasyfikacji „groźne / nie", ale nie do sprawdzenia hipotezy o czasie: żeby ocenić „czy to się dzieje po alarmie", trzeba widzieć CZAS każdego incydentu z osobna, a nie jedną zbiorczą liczbę.<br>**Pierścień w pamięci RTC (`rtcNvsFailLogTs`/`rtcNvsFailLogKey`, `NVS_FAILLOG_SLOTS`=8), nie w NVS.** Zapisywanie awarii NVS przez zapis do NVS jest cyrkularne — gdyby to właśnie ta pamięć akurat szwankowała, próbowalibyśmy dokumentować awarię w miejscu, które właśnie zawodzi. RTC przeżywa deep sleep (jedyny tryb snu, jaki urządzenie zna) i nie kosztuje ani jednego cyklu zapisu flash.<br>**Jedno miejsce notujące niepowodzenie** (`zanotujNvsFail()`), wołane z obu funkcji (`nvsPutStr`, `nvsPutU16`) zamiast duplikować logikę — D46 dopisywało `rtcNvsFailKey` w dwóch kopiach z rzędu, więc to jest naprawa własnej niedoróbki sprzed jednego commitu.<br>Wysyłka do `devices/<id>/nvsfaillog/<znacznik>` idzie **dokładnie tym samym wzorem co dziennik wieczka** (D-none, `lidlog`): własny klucz na paczkę (żeby druga synchronizacja nie nadpisała pierwszej), „kasowanie" dopiero po potwierdzonym 2xx — tu oznacza to przesunięcie `rtcNvsFailLogSent` do `rtcNvsFailLogTotal`, a nie fizyczne kasowanie (dane są w RTC, nie w NVS, więc nie ma czego kasować).<br>**Mutacja złapała słaby test.** Pierwsza wersja testu przepełnienia pierścienia sprawdzała tylko „czy najstarszy klucz zniknął z JSON-a" — i to przechodziło NAWET po usunięciu obcinania zakresu w `nvsFailLogJson()`, bo fizyczne nadpisanie slotu w pierścieniu i tak usuwa starą wartość, niezależnie od logiki obcinania. Prawdziwa stawka to LICZBA wpisów (bez obcięcia funkcja próbuje odtworzyć wszystkie `total-sent`, z czego nadmiarowe są duplikatami danych z nadpisanych slotów) — test poprawiony, żeby liczył wystąpienia, mutacja złapana.<br>**Aplikacja pokazuje historię na ekranie Historii pudełka, obok wybudzeń** — celowo NIE osobny ekran, żeby dwie listy czasów dało się porównać gołym okiem bez przełączania. Każdy wpis przechodzi przez **tę samą klasyfikację co D46** (`opisNvsFailKey()`) — jedna prawda o tym, co jest groźne, nie dwie osobne oceny w dwóch miejscach. Karta jest ukryta, dopóki nie ma historii — to narzędzie na sytuację wyjątkową, nie element do oglądania na co dzień.<br>Hipoteza Kuby o alarmie wciąż nieprzetestowana — to właśnie ta funkcja ma dać na nią odpowiedź, gdy incydent się powtórzy. |
| **D46** | „Pudełko zgłasza utratę danych" po każdym incydencie brzmiało tak samo groźnie — teraz mówi, **który klucz** nie zapisał się (zgłoszenie Kuby: *„to się pojawiło po nowej aktualizacji"*) | Trzeci raz ten sam ekran ostrzeżenia, trzeci raz inna przyczyna. Sprawdziłem najpierw, czy to powrót D39 (pusty zapis liczony jako awaria) — **nie jest**: ta naprawa siedzi w samym `nvsPutStr()`, więc żaden wywołujący nie może już jej ominąć; sprawdziłem wszystkie bezpośrednie `prefs.put*` w pliku i żaden nie omija wrappera. Sam licznik `rtcNvsFail` mówi tylko „coś przepadło" — nie mówi co, a to jest różnica między *nic się nie stało* (token logowania, wpis czarnej skrzynki — obie strony same się naprawiają przy najbliższym zapisie) a *zgubiona dawka* (wpis kolejki zdarzeń). Bez tego rozróżnienia każdy pojedynczy incydent — nieważne jak błahy — wyglądał tak samo groźnie jak utrata dawki, co jest dokładnie tą samą pułapką co „czekam" kontra „stoję" (D25, D32).<br>**Zamiast zgadywać czwarty raz z rzędu — pomiar.** `rtcNvsFailKey[10]` zapamiętuje nazwę klucza z **ostatniego** nieudanego zapisu (w `nvsPutStr()` i `nvsPutU16()` — obie funkcje osobno, sprawdzone mutacją, bo łatwo poprawić jedną i zapomnieć o drugiej), idzie do statusu jako `nvsFailKey`. Aplikacja klasyfikuje klucz wzorcem (`q\d+` = wpis kolejki, `n\d[sp]`/`netN` = lista WiFi — obie realnie groźne; `sched`/`dw`/`dex`/`tok`/`lb*`/`ll*` — same się naprawiają) i pokazuje **po imieniu**, co się stało, zamiast domyślnie straszyć kalendarzem przy każdym incydencie. Klucz **nierozpoznany** jest świadomie traktowany jako groźny — nowy kod w przyszłości nie ma prawa cicho stać się „bezpieczny" tylko dlatego, że nikt go nie dopisał do listy.<br>**Bez klucza (starszy firmware, zanim to wgrano) zostaje ostrożne ogólne ostrzeżenie, nie cisza** — brak informacji nie może wyglądać jak informacja o braku ryzyka.<br>Ten „1 zapis" ze zgłoszenia Kuby wciąż nie ma znanej przyczyny — to uczciwie hipoteza, nie rozwiązanie. Ale od `1.35.0` następny taki incydent powie wprost, co konkretnie nie wyszło, zamiast zostawiać samą liczbę. |
| **D42** | Sieci da się **usunąć i przełączyć z aplikacji** (prośba Kuby) | Lista sieci była do tej pory jednokierunkowa: dało się dodać, nie dało się nic cofnąć. Cztery miejsca zapełniały się na zawsze, a zła sieć na czele listy kosztowała 15 s radia przy każdym połączeniu.<br>Obie operacje idą **tą samą drogą co nowa sieć**: wpis `wifiCmd` w bazie, który pudełko wykonuje i kasuje. Żadnego „natychmiast" — pudełko śpi, więc zrobi to przy najbliższym połączeniu, i ekran mówi to wprost.<br>**„Przełącz" nie rozłącza niczego tu i teraz — przesuwa sieć na czoło listy.** Gdyby wskazana sieć okazała się nieosiagalna, pudełko zejdzie po liście do starej i nic się nie stanie. Rozłączanie na żądanie dawałoby tę samą korzyść i ryzyko urwania łączności w połowie operacji.<br>**Twarde zabezpieczenie: nie wolno usunąć jedynej sieci, przez którą pudełko właśnie rozmawia.** Zostałyby tylko poświadczenia sterownika, a te wskazują dokładnie tę samą sieć — więc efekt byłby zerowy albo, po ich nadpisaniu, katastrofalny. Firmware odmawia i mówi dlaczego (`WIFI_USUN_OSTATNIA`), a aplikacja w ogóle nie pokazuje wtedy przycisku. **Dwie warstwy, bo ta po stronie pudełka jest jedyną, której użytkownik nie ominie.**<br>Polecenie kasujemy z bazy **także wtedy, gdy się nie udało** — nieusuwalne polecenie próbowałoby się wykonać przy każdym połączeniu i nigdy nie dałoby się go odwołać. Wynik idzie do `netMsg`, więc widać, co się stało.<br>**Do handlera w HTML idzie INDEKS, nie nazwa sieci.** `esc()` nie escapuje apostrofu, a hotspot iPhone'a bywa nazwany „Kuba's iPhone" — taka nazwa wyszłaby z ciągu w atrybucie `onclick` i stała się kodem. Nazwę handler odczytuje sam, z bieżącej listy.<br>Przy okazji wydzielone `wifiListeZapisz()` / `wifiListeCzytaj()`: listy dotykają teraz trzy operacje i każda z własną kopią tego kodu byłaby trzecią okazją do pomyłki w tym samym miejscu.<br>**Mutacja pokazała test, który nic nie sprawdzał.** Kasowanie kluczy po skróconej liście opisałem jako obronę przed „zmartwychwstaniem" sieci — nieprawda, bo `netN` i tak ogranicza odczyt. Prawdziwy powód to odzyskanie miejsca w NVS (~20 kB na wszystko, D25). Komentarz i test poprawione tak, żeby mierzyły to, co naprawdę jest stawką. |
| **D41** | Lista sieci **nie odcina** poświadczeń sterownika; stan „otwarte" mówi, jak jest stary (naprawa regresji z D38) | Objaw od Kuby: *„mimo otwarcia aplikacja mi się nie odświeża, cały czas jest że otwarte jest pudełko i podłączone do ładowania, tak jakby łapał to co było przy wgraniu firmware"*. Czyli od wgrania firmware pudełko nie wysłało ani jednego statusu — nie łączyło się z siecią.<br>**Wada projektowa mojej zmiany:** `wifiConnect()` próbowało `WiFi.begin()` bez argumentów **wyłącznie przy pustej liście**. Tak pudełko łączyło się przez całe miesiące, a nowa funkcja zamieniła tę drogę na warunkową — więc **jeden błędny wpis na liście odcinał pudełko całkiem**, mimo poprawnych poświadczeń leżących obok w tej samej pamięci. Teraz poświadczenia sterownika są **ostatnią deską ratunku po wyczerpaniu listy**, zawsze.<br>**Zasada, którą sam złamałem, a która stoi w tym pliku przy portalu fizycznym: nowa droga nie może zabierać starej, dopóki nie udowodni, że działa.** Audyt pilnuje tego teraz statycznie (mutacja: powrót do `if (n == 0)` wywala kontrolę).<br>Do tego `WiFi.disconnect()` przed każdą kolejną próbą — bez tego drugie `WiFi.begin()` trafia w sterownik wciąż zajęty poprzednią siecią i bywa po cichu ignorowane, czyli przegląd listy jest tylko czekaniem na to samo niepowodzenie.<br>**Druga połowa zgłoszenia to aplikacja.** Baner „Otwarte od 5 h" liczył czas od `openSince` do **teraz**, więc rósł sam z siebie także wtedy, gdy pudełko od dawna milczało — i wyglądał jak wiadomość o stanie bieżącym. Teraz liczy do chwili, w której pudełko **ostatnio to widziało**, a po `OPEN_SWIEZOSC_H` = 3 h ciszy mówi wprost: *„Ostatnia wiadomość z pudełka: 6 godz. temu. Od tamtej pory milczy"*. Próg wzięty z faktów: przy otwartym wieczku pudełko melduje się co 15–30 min (`OPEN_WARN_FIRST_S`, `OPEN_WARN_REPEAT_S`), więc trzy godziny to już nie jest normalna przerwa. Świadomie mniej niż `MILCZY_PROG_H` = 26 h: tamten próg pyta, czy pudełko żyje, ten — czy **konkretny stan** jest jeszcze aktualny.<br>**Trzeci raz w tej sesji atrapa okazała się niewierna oryginałowi:** w `dom_stub` `textContent` i `innerHTML` były dwoma niezależnymi polami, a w przeglądarce to ta sama treść widziana z dwóch stron — ustawienie jednego kasuje drugie, a `textContent` escapuje `&`, `<`, `>` (ale **nie** cudzysłowy). Test wstrzyknięcia HTML pytał przez to o zły wskaźnik i musiał zostać zawężony do `onerror` w otwartym znaczniku — `onclick` w wyniku to własny, zaufany kod aplikacji. |
| **D39** | `nvsPutStr("")` to **sukces**, nie awaria (naprawa błędu z D36/D38, zgłoszonego przez Kubę) | Objaw: aplikacja krzyczała *„Pudełko zgłasza utratę danych — 2 zapisów się nie udało"*, a sieć WiFi wysłana z telefonu nie chciała się przyjąć mimo pięciu otwarć pudełka.<br>Przyczyna: **`Preferences::putString("")` zwraca ZERO — dokładnie tyle samo, co zapis nieudany.** Odróżnić tego się nie da, a ja w tej samej sesji dołożyłem trzy miejsca zapisujące potencjalnie puste napisy: hasło do sieci otwartej, hasło hotspotu i pustą listę wyjątków dawkowania. Każde podnosiło licznik utraty danych, a w przypadku sieci dodatkowo powodowało, że `wifiSiecDodaj()` zwracało false — więc hasło słusznie zostawało w bazie, tyle że **sieć była już zapisana poprawnie** i wyglądało to na awarię, której nie było.<br>Naprawa: pustej wartości nie zapisujemy wcale — kasujemy klucz. Przy odczycie wychodzi na to samo (`getString` oddaje wtedy wartość domyślną), a wynik jest jednoznaczny.<br>**Dlaczego testy tego nie złapały: atrapa była łagodniejsza od oryginału.** `putString` w `arduino_shim.h` zwracała `size()+1`, więc dla pustego napisu 1 zamiast 0 — i cała ta klasa błędów była dla testów niewidzialna. Atrapa poprawiona, sześć nowych kontroli, sprawdzone mutacją.<br>**To trzeci raz w tym projekcie ta sama lekcja** (B22, D30, teraz D39): *atrapa, która nie umie zawieść dokładnie tak jak oryginał, mierzy inną rzecz niż się myśli.*<br>Do statusu doszło `netMsg` — wynik ostatniej próby przyjęcia sieci prosto z pudełka — a `wifiNowa` niesie `ts`. Dzięki temu aplikacja odróżnia *„pudełko jeszcze się nie łączyło"* (czekaj) od *„łączyło się i sieci nie przyjęło"* (sprawdź nazwę). Bez tego ekran mówił „czeka" także wtedy, gdy nic już nie miało przyjść — i właśnie na to Kuba patrzył. **Zamiast zgadywać czwarty raz, dołożony jest pomiar.** |
| **D38** | Pudełko pamięta **listę sieci**, a nową dostaje **z aplikacji** (prośba Kuby) | Wcześniej było tu samo `WiFi.begin()` bez argumentów — jedna sieć zapamiętana przez sterownik, do zmiany wyłącznie portalem przy otwartym pudełku. Na wyjeździe to znaczy: otwórz pudełko, przytrzymaj przycisk, zresetuj, połącz telefon z `PillBox-setup`.<br>**Paradoks „żeby dodać sieć, trzeba mieć sieć" znika przez hotspot.** Na liście stoi na stałe hotspot z telefonu Kuby; na wyjeździe wystarczy go włączyć, pudełko się z nim łączy i dopiero wtedy odbiera sieć hotelową. To był jego pomysł i jest trafny — dlatego wprost o tym pisze ekran w aplikacji, a nie tylko ten dziennik.<br>**Kolejność prób zaczyna się od tej, która zadziałała ostatnio** (`rtcNetOstatnia`), więc w domu to nadal JEDNA próba i ani milisekundy radia więcej. Kolejne sieci dostają tylko `WIFI_ALT_TIMEOUT_MS` — są na wypadek wyjazdu, a nie po to, żeby przy każdej awarii routera trzymać radio przez minutę.<br>Osobne klucze NVS na SSID i hasło, a nie jeden sklejony napis: **hasło do WiFi może zawierać dowolny znak, łącznie z tym, którego użylibyśmy jako separatora.**<br>**Hasło leży w bazie tylko do najbliższego połączenia** — pudełko kasuje `wifiNowa` po odebraniu. Reguły i tak pilnują dostępu, ale krótszy czas życia jest tańszą obroną niż każda inna.<br>**Kolejność jest tu całą treścią (zasada 6):** najpierw zapis do NVS, potem sprawdzenie, czy się udał, i DOPIERO wtedy kasowanie z bazy. Odwrotnie stracilibyśmy sieć, której nikt już nie zna — a wraz z nią jedyną drogę do pudełka poza portalem. Dlatego `wifiSiecDodaj()` **zwraca wynik**, a test z `failKeys` pilnuje, że nieudany zapis melduje porażkę zamiast ją przemilczeć (mutacja: `return true` wywala dwa testy).<br>Portal fizyczny **zostaje** i też dopisuje do listy — jedno źródło prawdy i ostatnia deska ratunku, gdy lista się rozjedzie.<br>Status niesie `nets` z samymi NAZWAMI sieci, nigdy hasłami; aplikacja pokazuje dzięki temu trzy różne stany: „czeka na pudełko", „pudełko zna", „nigdy się nie odezwało". Osobny test pilnuje, że **hasło nie trafia na ekran**. |
| **D37** | Pudełko też zna rozpisanie i **milczy w dniu bez leku** (dokończenie D36) | Aplikacja liczyła dzień odstawienia poprawnie, ale pudełko dalej dzwoniło o 20:00 i wysyłało „missed". **To nie jest kwestia wygody: dzwonek w dniu odstawienia przed zabiegiem zachęca do wzięcia tabletki, której brać nie wolno.**<br>Firmware dostał `rtcDoseWeek[7]` + do `DOSE_EX_MAX` wyjątków na daty, w dziesiątych częściach tabletki (10 = jedna, 5 = połowa), pobierane w `fetchConfig()` i trzymane w NVS — bo po twardym restarcie pamięć RTC jest pusta, a pierwsze wybudzenie po resecie nie może zadzwonić w dniu odstawienia.<br>**`DOSE_NIEZNANA` (255) znaczy „nie wiem" i nigdy „zero".** `dzisBezLeku()` zwraca prawdę wyłącznie przy **pewnym** zerze: bez zegara, bez rozpisania albo przy danych nie do odczytania — dzwonimy. W razie wątpliwości lepiej przypomnieć o dawce niepotrzebnie niż przemilczeć potrzebną.<br>Rozpisanie niepełne odrzucamy w całości, **tak samo i z tego samego powodu co w aplikacji** — obie strony mają tę samą regułę, bo obie mogą dostać te same uszkodzone dane.<br>Z bazy bierzemy tylko wyjątki **od dziś w przód** i najwyżej `DOSE_EX_MAX` najbliższych: przeszłe rozlicza aplikacja, a pamięć RTC jest za cenna na rok wstecz.<br>`checkDayRollover()` również pomija doby bez leku — inaczej każdy dzień odstawienia wracałby do kalendarza jako czerwony okrężną drogą, przez kolejkę.<br>Obie poprawki sprawdzone **mutacją**: usunięcie warunku w rolowaniu i podmiana odrzucania niepełnego rozpisania na „brakujące dni = zero" — każda wywala po dwa testy. |
| **D29** | Nieudany zapis dawki ma **własny sygnał dźwiękowy** (naprawa **B24**) | Podwójny sygnał błędu — wyraźnie inny od pojedynczego potwierdzenia kolejki. Znaczy dokładnie: *„nie mam tej dawki nigdzie, zapisz ją ręcznie"*. Notatka w czarnej skrzynce to `DAWKA NIEZAPISANA`, a log mówi wprost, że dawka przepadła, zamiast twierdzić, że została zapisana.<br>**Dlaczego to musi być dźwięk, a nie tylko licznik w aplikacji:** bez internetu aplikacja o niczym nie wie — pudełko odzywa się wyłącznie brzęczykiem. Licznik `nvsFail` dojedzie do telefonu dopiero po powrocie sieci, czyli po fakcie i po tym, jak dawka już przepadła bez śladu.<br>Audyt pilnuje teraz trzech rzeczy naraz: że `reportEvent()` nie wyrzuca wyniku `queuePush()` do kosza, że go zapamiętuje i że dźwięk od niego zależy. Sprawdzone mutacją.<br>**Morał, i to najważniejszy z całego dnia: opis objawu od Kuby („był ten dźwięk") był twardszym dowodem niż całe moje wnioskowanie z liczników w bazie.** Dźwięk jednoznacznie wskazywał jedną gałąź w kodzie — a ja z `dropped = 0` wyciągnąłem wniosek o kontaktronie, czyli o zupełnie innym miejscu. Czwarty raz w tym projekcie i zapisane w `CLAUDE.md` od początku. |
| **D23** | Odzwoniony alarm zostawia znacznik „ta doba załatwiona" (naprawa **B17**) | `oznaczAlarmObsluzony()` / `alarmJuzObsluzony()` — nowy `rtcAlarmDoneDay` w pamięci RTC **i w NVS**, więc przeżywa reset. Ustawiany w dwóch miejscach: po wyczerpaniu prób (zapisane „missed") i po wzięciu tabletki w trakcie dzwonienia.<br>Bez zegara działa tak samo jak `juzDzisBrane()` — na czasie od zdarzenia, w oknie `ONE_DOSE_WINDOW_S`.<br>**Dlaczego nie skrócić po prostu `MATCH_WINDOW_MIN`:** to okno ma sens tam, gdzie jest — decyduje, czy otwarcie pudełka zalicza się do danej pory leku. Problemem nie była jego szerokość, tylko brak pamięci o tym, że alarm już się odbył.<br>**Morał, trzeci raz w tym projekcie:** błąd ujawnił się wyłącznie po dłuższym czasie bez sieci. Wszystkie trzy ostatnie (B16, B17 i to, co znalazłem przy okazji w audycie) miały tę samą cechę — z internetem ścieżka wygląda inaczej i wygląda dobrze. |
| **D22** | Ochrona przed drugą dawką działa **bez zegara** — na czasie od poprzedniej dawki (naprawa **B16**) | Pudełko bez NTP nie wie, który jest dzień, ale **potrafi zmierzyć, ile czasu minęło**: zegar RTC tyka także w deep sleepie, a różnica dwóch odczytów jest poprawna nawet gdy sama data jest zmyślona. Do pytania „czy brałeś to niedawno" różnica w zupełności wystarcza.<br>Nowy `rtcTakenTs` zapisywany **zawsze**, niezależnie od stanu zegara. `zapiszDawke()` i `juzDzisBrane()` wydzielone jako testowalne funkcje — sześć miejsc bramkowanych `rtcTimeValid` chodzi teraz przez jedną decyzję.<br>**Kluczowe rozróżnienie:** z wiarygodnym zegarem mamy PEWNOŚĆ powtórki, więc otwarcie jest pomijane, jak dotąd. Bez zegara to tylko **podejrzenie** — nie wiemy, gdzie przebiega granica doby, więc branie o 23:00 i kolejne o 10:00 wyjdzie jako „już brane", choć to nowa doba. Dlatego bez zegara **ostrzegamy, ale otwarcia NIE wyrzucamy**: fałszywe pikniecie kosztuje sekundę, a zgubiona dawka — dzień w kalendarzu.<br>Okno `ONE_DOSE_WINDOW_S` = 20 h, nie 24: przy braniu o różnych porach doba lekowa i tak się przesuwa.<br>Gdy sieć wróci, `syncTimeNTP()` przelicza dawkę zapisaną „na ślepo" na właściwą dobę — wie, ile czasu temu była, i wie już, która godzina.<br>**Zostaje jedno ograniczenie, którego usunąć się nie da:** po **całkowitym zaniku zasilania** bez internetu pudełko nie ma jak stwierdzić, ile czasu minęło — pamięć RTC i zegar zerują się razem. Wtedy ochrona rusza od nowa. |
| **D15b** | Decyzja o potwierdzeniu dawki wydzielona z `runAlarmWindow()` (`alarmPotwierdzony()`) | **Zachowanie niezmienione** — funkcja zwraca dziś dokładnie to samo co wcześniej, razem z błędem **B1**. Chodzi wyłącznie o możliwość testowania: pętla alarmu to `millis()`, `delay()` i odczyt pinu, więc nie da się jej uruchomić w teście; decyzja da się. Bez tego którykolwiek z trzech wariantów naprawy **B1** wszedłby do najwrażliwszego miejsca w projekcie **bez ani jednego testu**. Teraz każdy wariant to zmiana jednej linii z gotowym zestawem testów obok, a audyt pilnuje, żeby pętla nie omijała funkcji skrótem do pinu — inaczej naprawa weszłaby w jedno miejsce, a działałoby drugie. |
| **D14** | Nieudany zapis do NVS jest **liczony i widoczny** (naprawa **B5**) | `nvsPutStr()` / `nvsPutU16()` sprawdzają wynik zapisu i podbijają `rtcNvsFail`. Licznik jedzie w statusie jako `nvsFail`, aplikacja krzyczy przy wartości > 0.<br>Osobno: `queuePush()` podnosi licznik kolejki **tylko wtedy, gdy treść naprawdę weszła**. Wcześniej nieudany zapis zostawiał wpis-widmo — `queuePeek()` nie miał czego odczytać, wysyłka stawała w miejscu, a licznik nigdy nie schodził do zera. To był drugi sposób na zatkanie kolejki, obok B3.<br>Liczniki żyją w pamięci RTC, więc zerują się po odłączeniu zasilania — status idzie w każdym wybudzeniu, więc okno na zauważenie jest wystarczające, a NVS to akurat ta pamięć, której w tym scenariuszu nie ufamy. |
| **D8** | `CHARGE_AWAKE_MAX_S` = 4 h | Pudełko na kablu czuwa, żeby dało się w tym czasie coś dograć. 4 h to bezpiecznik na wypadek, gdyby nie zauważyło odłączenia — z włączonym radiem zjadłoby ogniwo |
| **D7** | `config.h` **jest** w repo, z placeholderem hasła | Kuba pobiera folder i otwiera wprost w Arduino IDE. Bez `config.h` szkic się nie otwiera — ręczna zmiana nazwy przed każdym wgraniem, na każdym komputerze osobno |
| **D4** | Dziennik wieczka ma **własny** bufor, nie `queuePush()` | Kolejka przy zapełnieniu nadpisuje najstarszy wpis. Wieczko trzęsące się w plecaku wyrzuciłoby z niej **zapisy dawek** |
| **D3** | Dziennik wysyłany pod osobnymi kluczami paczek | Pudełko kasuje po wysłaniu, więc wspólny węzeł = druga synchronizacja nadpisuje pierwszą. Po kilku dniach wyjazdu zostałby ostatni dzień |
| **N2** | `rtcStuckOpen` usunięty | Licznik zwiększany w dwóch miejscach i **nigdy przez nic nieczytany**. Skan potwierdził, że był jedyny taki w firmware |
