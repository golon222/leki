# Decyzje — Dane, kolejka, dawkowanie

zapisy do bazy, uzgadnianie dni, dzień lekowy, rozpisanie dawki

Indeks wszystkich decyzji: `DECYZJE.md`. Tu są **pełne** wpisy z tego obszaru (17 szt.), od najnowszego.

| # | Decyzja | Co by było bez tego |
|---|---|---|
| **D88** | **Doba domyka się o granicy doby lekowej, nie o północy zegara — i znacznik dawki potrafi wreszcie zawieść głośno** (firmware `1.47.1`) | **Znalezione przy przeglądzie kodu, nie zgłoszone z terenu** — jak D79, i z tego samego powodu groźne: objaw nie miał jak wyjść na ekran.<br>**1. `checkDayRollover()` zamykało dobę, która wciąż trwała.** Punkt odniesienia liczył się jako `now − localMinutesOfDay(now)`, czyli od północy **zegara**. Między północą a `DAY_START_HOUR` te dwie północe należą do różnych dób: o 1:30 doba lekowa jest jeszcze **wczorajsza** (tabletkę wolno wziąć do 3:00), a punkt odniesienia przeskakiwał już na dziś. Pierwszy krok pętli — „23:59 poprzedniego dnia" — trafiał wtedy w dobę **bieżącą** i pudełko zgłaszało za nią `missed`. Potem, po granicy o 3:00, zgłaszało ją **drugi raz**, bo `setRolloverDay(today)` przesunął znacznik na dobę już rozliczoną.<br>**Kiedy to się dzieje:** znacznik `rollDay` musi być starszy niż bieżąca doba, czyli pudełko przespało całą dobę — rozładowane ogniwo, wyjazd, martwe urządzenie — a wraca do życia między północą a 3:00. Na ładowarce melduje się **co minutę** (`CHARGE_POLL_S`), więc wystarczy podłączyć kabel w nocy.<br>**Dlaczego nikt tego nie zobaczył:** aplikacja nie maluje takiego dnia na czerwono, bo dzień lekowy rozstrzyga się z końcem doby (**D64**) — wpis `missed` na dziś daje status „jeszcze nie", nie „pominięte". D64 przykryło objaw; błąd został w danych. Duplikat też jest niewidoczny, bo `reconcileDecyzja()` pozwala `missed` **tylko tworzyć**, nigdy nadpisywać. W bazie zostawały jednak dwa zdarzenia o tym samym dniu, a pierwsze z nich było po prostu nieprawdą w chwili zapisu.<br>**Naprawa to odjęcie `DAY_START_HOUR` PRZED policzeniem północy.** Data lokalna tak przesuniętej chwili to dokładnie numer bieżącej doby lekowej, więc pierwszy krok pętli zawsze trafia w dobę **poprzednią** — pierwszą, która naprawdę jest zamknięta. Znacznik staje wtedy na dobie trwającej i ta domyka się później, dokładnie raz.<br>**Testy sprawdzają WŁASNOŚĆ, nie trzy godziny.** Pierwsza wersja testów miała pojedyncze przypadki (1:30, 3:01, 23:50) i **przepuszczała pomyłkę o jedną godzinę** — sprawdzone mutacją. Teraz pętla przechodzi całą dobę co 17 minut i pilnuje dwóch rzeczy naraz: doba trwająca nie jest **nigdy** zgłaszana jako pominięta, a doba zamknięta jest zgłaszana **dokładnie raz**. Cztery mutacje punktu odniesienia, każda złapana.<br>**2. `takenDay`, `rollDay` i `almDay` szły do NVS bez sprawdzania wyniku.** `prefs.putULong()` zwraca liczbę zapisanych bajtów albo zero — i tego zera nikt nie czytał. Stawka nie jest diagnostyczna: **`takenDay` to jedyny ślad wziętej dawki, który przeżywa RESET.** Po wgraniu programu albo zaniku zasilania pamięć RTC jest pusta i `juzDzisBrane()` czyta wyłącznie ten wpis. Zapis, który przepadł po cichu, znaczy więc: pudełko otwarte drugi raz tego samego dnia **nie ostrzega przed powtórną dawką Warfinu** — a licznik `nvsFail` nawet nie drgnął, więc aplikacja nie miała o czym krzyczeć. Dokładnie ta klasa cichej straty, dla której powstało **D46**.<br>`nvsPutU32()` idzie tą samą drogą co `nvsPutStr`/`nvsPutU16`: wynik do licznika, klucz do historii. Aplikacja zna teraz te trzy klucze z nazwy — `takenDay` jako **groźny** (dotyczy leku), `rollDay` i `almDay` jako niegroźne (dzień zgłoszony dwa razy, przypomnienie odegrane dwa razy).<br>**Przy okazji naprawiona atrapa, i to jest tu ważniejsze niż sama poprawka.** `putULong()` i `putUInt()` w `arduino_shim.h` zwracały `void`, czyli **nie umiały zawieść** — cała gałąź „zapis się nie udał" była dla testów niewidzialna i dlatego luka mogła przeżyć 314 kontroli audytu. Piąty raz ta sama lekcja co **D30**, **D39** i **D85**: atrapa łagodniejsza od oryginału mierzy nie to, co trzeba.<br>**Podział pracy między testem a audytem jest tu z premedytacją (lekcja B19):** testy sprawdzają arytmetykę, audyt — że odjęcie granicy doby w ogóle stoi w kodzie i że trzy klucze naprawdę idą przez sprawdzany zapis. 17 nowych kontroli, 6 mutacji, każda złapana. Kompilacja obiema ścieżkami: 64% flasha. |
| **D87** | **Dawka zapisana bez zegara odzyskuje datę, gdy zegar wróci** (firmware `1.47.0`) | Pytanie Kuby: *„czy jak pudełko nie ma wifi, to nadal zapisuje dawki?"*. Zapisuje — ale przy okazji odpowiedzi wyszło, że **w jednym przypadku zapisuje je bez daty, i to bezpowrotnie**.<br>`makeRecord()` stało: `rtcTimeValid ? time(nullptr) : 0`. Bez NTP wpis szedł do kolejki z **twardym zerem** — i takie docierało do bazy. Zdarzenie bez daty aplikacja umie pokazać w „Zdarzeniach z pudełka", ale nie ma go jak umieścić w kalendarzu: dzień, w którym tabletka **była** wzięta, wygląda wtedy dokładnie jak pominięty. Przy leku przeciwzakrzepowym to nie jest kosmetyka.<br>**A informacja była na wyciągnięcie ręki.** Zegar RC tyka także w deep sleepie, więc `time(nullptr)` bez NTP zwraca **sekundy od startu płytki** — nie datę, ale **poprawny upływ czasu**. Wystarczy go zapisać zamiast zera i poczekać: przy pierwszej synchronizacji `before` (odczyt na starej, fałszywej skali) i `time(nullptr)` (na prawdziwej) dają różnicę, która przenosi wpis dokładnie tam, gdzie powinien być.<br>**To nie jest nowy pomysł — to ten sam ratunek, który od dawna działa dla `rtcTakenTs`** w `syncTimeNTP()` („czy brałem dziś"). Był zastosowany do jednego znacznika w pamięci RTC i nikt nie zauważył, że kolejka, czyli jedyna droga dawki do kalendarza, została bez niego. `queueNadajCzas()` robi dla niej to samo.<br>**Twardy reset unieważnia skalę i to musiało dostać własną obsługę.** Sekundy od startu mają sens tylko w obrębie jednego startu; po resecie licznik rusza od zera i wpis „3600" sprzed resetu jest nieodróżnialny od „3600" po nim. Przeliczony jak świeży wpisałby dawkę o kilka godzin za późno. **Fałszywa data jest gorsza niż jej brak** — kalendarz pokazywałby wtedy nieprawdę z pełnym przekonaniem. Dlatego `queueEpokaSkasuj()`, wołane z `setup()` gdy pamięć RTC jest pusta, zeruje te znaczniki: „nie wiem kiedy" zostaje uczciwie „nie wiem kiedy". Kasujemy **datę, nigdy zdarzenie** — wpis zostaje w kolejce w całości.<br>**Do bazy znacznik względny nie wychodzi jako data.** `tsDoBazy()` zamienia go z powrotem na zero, dopóki prawdziwej nie ma — inaczej dawka lądowałaby w kalendarzu w roku 1970, czyli gorzej niż nigdzie. Zachowanie widziane z zewnątrz jest więc identyczne jak dotąd; nowa jest tylko **szansa**, że do wysyłki wpis zdąży dostać właściwą datę. Zdąża praktycznie zawsze, bo `wifiConnect()` synchronizuje zegar **przed** `flushQueue()`.<br>**Czego świadomie NIE zrobiono:** kotwicy dnia z `rollDay` dla wpisów, które mimo wszystko zostały z zerem. Po tej zmianie taki wpis wymaga **dwóch** twardych resetów w jednym okresie bez sieci — a kotwica działałaby tylko wtedy, gdy dolna i górna granica wypadają w tym samym dniu lekowym. Więcej kodu na ścieżce zapisu dawki niż pożytku.<br>**Żadnego nowego pola w bazie** (ograniczenie 7 nietknięte) i żadnej zmiany w aplikacji.<br>32 testy jednostkowe i 13 kontroli audytu. Kontrole audytu są tu osobno **z premedytacją**: testy sprawdzają arytmetykę, audyt sprawdza **okablowanie**. To dokładnie ta różnica, która kosztowała nas **B19** — backoff liczył się poprawnie, miał własny test, a licznik nie miał jak urosnąć, bo nikt go nie podnosił. `queueNadajCzas()` może być bez zarzutu i zupełnie bezużyteczna, jeśli nikt jej nie zawoła. Sprawdzone 11 mutacjami; każda złapana. |
| **D79** | **Historia rozpisania dawki — zmiana planu obowiązuje OD DNIA zmiany, nie wstecz** (aplikacja `2026-08-21.54`) | **Znalezione przy przeglądzie, nie zgłoszone — i dlatego groźne.** `dawkaNaDzien()` liczyła plan dla **każdego** dnia z rozpisania obowiązującego w tej chwili. Przy warfarynie lekarz koryguje dawkę po wynikach INR, więc **pierwsza taka korekta przepisywała całą przeszłość**: dzień, w którym Kuba wziął jedną tabletkę przy planie jednej, po zmianie planu na 1,5 robił się wstecz „mniejszą dawką" — żółty w kalendarzu, gorszy w skuteczności i taki sam w raporcie dla lekarza. Nikt by tego nie zauważył, dopóki nie spojrzałby na kalendarz sprzed zmiany.<br>**Historia mieszka pod użytkownikiem (`users/<uid>/dosePlans`), nie w konfiguracji urządzenia — i to nie jest szczegół.** Pudełko pobiera `config.json` **w całości** i parsuje go w RAM-ie ESP32; rosnąca lista wersji planu pewnego dnia przewróciłaby mu odczyt ustawień. Pudełku wystarczy plan bieżący, który dostaje jak dotąd.<br>**Zasady:** wyjątek na konkretną datę wygrywa ze wszystkim (to najświeższa wiedza o tym dniu); dzień zamknięty bierze plan z przeszłości, dziś i przyszłość — bieżący; brak historii znaczy „nic nie wiemy o przeszłości" i zostaje zachowanie sprzed zmiany, czyli **zero regresji** dla dotychczasowych danych; nowa wersja zapisuje się tylko wtedy, gdy plan **naprawdę** się zmienił, żeby historia nie zrobiła się dziennikiem klikania „Zapisz".<br>Widać ją w Ustawieniach → Lek (zwijana lista „od kiedy — ile") i w raporcie dla lekarza jako osobna tabela: dawka 1 w czerwcu i 1,5 w sierpniu to nie ta sama terapia, choć obie są „pełne". 22 kontrole. |
| **D64** | **Dzień lekowy rozstrzyga się z końcem doby, a nie z ostatnim przypomnieniem** — „missed" z pudełka nie maluje już dzisiejszej kratki na czerwono ani nie zaniża skuteczności | Zgłoszenie Kuby: *„jak mam alarm o 20 i on przestanie dzwonić, to w kalendarzu dawka pokazuje się jako nie wzięta, a to dopiero powinno się pokazywać z końcem dnia"*. Ma rację i to nie jest sprawa estetyki.<br>**Zderzyły się tu dwie rzeczy, które w tym projekcie od dawna są rozdzielone — i jedna z nich cicho udawała drugą.** Pudełko wysyła `missed` po **ostatnim przypomnieniu doby** (D56) i to jest poprawne jako zdarzenie: „o 20:06 tabletki jeszcze nie było". Ale **przypomnienie nie jest porą brania leku** (CLAUDE.md 4b) — Kuba bierze Warfin kiedy chce, także o 22:00 albo o 1 w nocy. Kalendarz czytał to zdarzenie jako **werdykt na cały dzień**, choć doba lekowa trwała jeszcze siedem godzin.<br>**Szkoda była podwójna.** Kratka robiła się czerwona, a **skuteczność spadała** — i obie liczby **cofały się same**, gdy tabletka poszła dwie godziny później. Ekran straszył zamiast informować, przy leku przeciwzakrzepowym; a liczba, którą ogląda lekarz, przez pół wieczoru pokazywała nieprawdę.<br>**Reguła po naprawie jest jedna i obowiązuje w trzech miejscach** (kalendarz, pierścień skuteczności, raport dla lekarza): **wzięta dawka rozstrzyga dzień natychmiast; niewzięta — dopiero po zamknięciu doby lekowej** (`dzienZamkniety()`, czyli granica `DAY_START_HOUR`, którą `todayKey()` już zna). Trwający dzień bez dawki wygląda dokładnie tak jak pusta kratka — bo to jest ten sam stan: **jeszcze nie wiadomo**.<br>**Wyjątek, świadomy: wpis RĘCZNY wygrywa także dziś.** Jeśli sam zaznaczyłeś „nie wzięte", wiesz lepiej niż reguła — ta sama zasada, która od dawna rządzi dniami sprzed `trackingSince`. Rozróżnia je `source`, więc `dayDose()` zwraca teraz także `reczne`.<br>**Czego NIE zrobiono, choć kusiło:** żadnej zmiany w firmware. Zdarzenie `missed` zostaje takie, jakie jest — jest prawdziwe i widać je w „Zdarzeniach z pudełka". Zmieniła się **interpretacja**, nie pomiar. Alternatywa (wybudzenie o 3:00 tylko po to, żeby wysłać werdykt) kosztowałaby prąd co dobę i przeniosłaby regułę w miejsce, w którym trudniej ją zmienić.<br>**Lekcja, czwarta odsłona tej samej w jednym dniu (D25, D32, D61, D63):** „jeszcze nie" to **osobny stan**, nie łagodniejsza odmiana „nie". Za każdym razem, gdy te dwa zostały sklejone, ekran kłamał — i za każdym razem kłamał **na niekorzyść**, czyli w stronę, w którą użytkownik uczy się go ignorować. |
| **D36** | Dawka przestaje być **jedną liczbą** — jest rozpisaniem tygodniowym plus wyjątkami na konkretne dni (prośba Kuby) | Warfaryny prawie nigdy nie bierze się równo: lekarz rozpisuje tydzień nierówno (1 tabl. codziennie, w środy pół), a przed zabiegiem odstawia ją na kilka dni. Aplikacja miała na to **jedną liczbę `defaultDose`**, więc licznik zapasu i raport dla lekarza pokazywały tę liczbę wstecz na wszystkie dni — czyli **kłamały**, i to nie o drobiazg.<br>Trzy poziomy, od szczegółu do ogółu: `doseDays[data]` → `doseWeek[dzień tygodnia]` → `defaultDose`. Trzeci zostaje **na zawsze**, nie jako dług: w bazie Kuby leży dziś tylko on i nowa aplikacja musi działać, zanim cokolwiek ustawi.<br>**Indeks 0 to niedziela — konwencja `Date.getDay()` i `tm_wday`, wspólna z firmware**, żeby pudełko i aplikacja liczyły ten sam dzień bez przeliczania. Jedynym miejscem, gdzie spotyka się z ludzką kolejnością „od poniedziałku", jest `DOW_KOLEJNOSC` w warstwie widoku.<br>**Schemat niekompletny albo uszkodzony odrzucamy w CAŁOŚCI i wracamy do `defaultDose`.** Wyciąganie z niego pojedynczych dni byłoby gorsze niż nieużywanie go wcale: brakujące pole odczytane jako zero to **cichy dzień bez leku przeciwzakrzepowego**. To jest najważniejszy test w tej sekcji i pilnują go trzy osobne kontrole (brak dnia, śmieć zamiast liczby, wartość poza zakresem reguł).<br>Dzień z zerem dostał **własny status `off`** — nie „pominięty" i nie „brak danych". Wypada z licznika i z mianownika **w czterech niezależnych miejscach, każde z własną pętlą**: pierścień skuteczności w kalendarzu, `analyze()`, `inrContext()` i `collectRows()` w raporcie. Bez tego zaplanowana przerwa zaniżałaby skuteczność w dokumencie dla lekarza, czyli kłamała na niekorzyść — dokładnie ten błąd, który już raz naprawialiśmy dla dni sprzed uruchomienia pudełka.<br>**Ale gdy wpis mówi, że tabletka jednak poszła, wygrywa to, co się STAŁO, nad tym, co było zaplanowane** — dzień pokazuje się normalnie.<br>Prognoza zapasu przestała być dzieleniem: idzie dzień po dniu i odejmuje tyle, ile tego dnia przypada. Przy schemacie z zerami średnia tygodniowa myliła się o kilka dni, a to jest liczba, na podstawie której Kuba decyduje, kiedy iść po receptę. Horyzont 400 dni, bo schemat z samymi zerami nie kończy się nigdy.<br>Wyjątek zapisujemy **pod pojedynczą datą**, nie jako cały obiekt `doseDays` — nadpisanie całości kasowałoby wyjątki dopisane w międzyczasie z drugiego urządzenia (rodzina B27/B28/B29).<br>Tydzień rozpisany równo i zgodnie z dawką standardową **kasujemy z bazy**, żeby nie leżały tam dwa źródła tej samej prawdy i żeby pole dawki standardowej dalej na coś wpływało. |
| **D35** | Decyzja „nadpisać czy nie" przeniesiona z lokalnej kopii `doses` do transakcji na żywym stanie serwera (naprawa **B29**, zamyka całą rodzinę **B27/B28**) | Zamiast gonić kolejną przyczynę nieaktualności lokalnego stanu, usuwam zależność od niego całkowicie. `doReconcile()` dalej liczy LOKALNIE, który zapis jest kandydatem — to tylko **propozycja**, żeby nie odpalać transakcji na ścieżkach, które i tak nic by nie zmieniły. Ostateczna decyzja zapada w `reconcileDecyzja()`, wywołanej przez `runTransaction()` z **prawdziwą, bieżącą wartością serwera** w chwili próby zapisu — nie z tym, co telefon akurat sądzi, że tam jest.<br>Zamiast jednego zbiorczego `update()` na wszystkie kandydatury naraz, każda doba dostaje **własną, niezależną transakcję** — to świadome porzucenie atomowości między dniami, bo dni i tak są od siebie niezależne, a rozdzielenie na osobne transakcje jest właśnie tym, co zamyka wyścig.<br>Zachowany mechanizm ponowienia bez `openTs` przy przestarzałych regułach (dawne D-nieoznaczone, teraz w `zapiszReconcile()`) — ta sama logika, przeniesiona per-ścieżka.<br>**Morał tego dnia, powtórzony trzeci raz: łatanie KOLEJNEJ przyczyny tej samej nieaktualności nie kończy się, dopóki decyzja w ogóle zależy od stanu, który może być nieaktualny.** Poprawny fix nie szuka czwartej przyczyny — usuwa samą możliwość, żeby przyczyna miała znaczenie.<br>Atrapa Firebase w testach dostała `runTransaction()` odwzorowujące prawdziwą semantykę: funkcja aktualizująca dostaje bieżącą wartość z `__db.data` (nie z lokalnego stanu testu), zwrócenie `undefined` porzuca transakcję. Nowe testy odtwarzają wprost scenariusz Kuby: serwer ma poprawny ręczny wpis, lokalna kopia `doses` jest celowo pusta — i sprawdzają, że **żaden zapis nie idzie**. Sprawdzone mutacją z trzech niezależnych stron: logiki decyzji, wywołania transakcji i samej atrapy. |
| **D34** | `boot()` wysyła zaległą kolejkę **na starcie**, nie tylko przy powrocie łączności (naprawa **B28**) | `oczekWyslij()` idzie teraz jako pierwsza linia `boot()`, przed rejestracją jakiegokolwiek `onValue`. Kolejność ma znaczenie z tego samego powodu co w **D33**: gdyby nasłuch `doses` zdążył przeczytać stan serwera, zanim kolejka zdążyła go poprawić, powstałby dokładnie ten sam wyścig, który już raz naprawialiśmy.<br>**Trzeci mechanizm bezpieczeństwa w kalendarzu, każdy chroni przed inną awarią, żaden osobno nie wystarczał:** `zapiszPewnie()` kolejkuje zapis, który nie przeszedł (D1); `doReconcile()` nie nadpisuje tego, co już jest (D33); `boot()` daje kolejce szansę, zanim ktokolwiek na nią spojrzy (D34). Brakowało trzeciego ogniwa — łańcuch był tak mocny, jak jego najsłabsze.<br>**Test tej naprawy sam był pierwszą ofiarą pułapki, którą opisuje.** Komentarz przy poprawce pisał nazwę funkcji z nawiasami w prozie, regex testu złapał tę prozę zamiast prawdziwego wywołania, a mutacja usuwająca poprawkę nie wywaliła testu — dokładnie ten scenariusz, przed którym `strip()` w audycie firmware chroni od miesięcy, tylko po stronie aplikacji nikt jeszcze na niego nie trafił. Test poprawiony: usuwa komentarze blokowe przed szukaniem wywołania. |
| **D33** | Zdarzenie „pominięte" wolno **tylko założyć** brakujący wpis, nigdy zmienić istniejący (naprawa **B27**) | Zamiast `if (cur && cur.status === "taken") continue` stoi teraz `if (cur) continue`. Cokolwiek już jest w kalendarzu — zostaje.<br>To bezpieczny kierunek i da się go obronić bez znajomości wyścigu: „pominięte" jest **domyślnym pesymistycznym odczytem**, a jedyne, co uprawnia do jego unieważnienia, to otwarcie pudełka albo człowiek. Automat nie ma powodu, żeby kiedykolwiek **zmieniać** dzień na gorszy.<br>**Różnica jest zasadnicza: poprzednia wersja polegała na tym, że lokalna kopia `doses` jest świeża. Ta nie polega na niczym** — nie da się jej ominąć wyścigiem, bo nie ma warunku, który wyścig mógłby przekłamać.<br>Test odtwarza wprost przypadek z nieaktualną kopią i sprawdza, że nie powstaje ŻADEN zapis. Osobno pilnowane, że brakujący dzień nadal dostaje „pominięte" i że otwarcie nadal bije pominięte. Sprawdzone mutacją. |
| **D32** | `zapiszPewnie()` odróżnia **odmowę bazy** od **braku sieci** (przy okazji **B27**) | Odmowa dostaje własny, czerwony komunikat: *„BAZA ODRZUCIŁA zapis, to nie minie samo"* i podnosi `staleRules`, więc w Ustawieniach zapala się wyjaśnienie, co z tym zrobić. Brak sieci zostaje przy spokojnym *„czeka na połączenie, nie zginie"* — bo tam to jest prawda.<br>**Zasada, trzeci raz w tym projekcie: komunikat, który brzmi tak samo przy „poczekaj" i przy „to się nigdy nie stanie", jest gorszy niż jego brak** — bo aktywnie usypia. Dokładnie tak samo kłamał `beepQueued()` w pudełku (**B24**) i `flushQueue()` przy zatkanej kolejce (**B3**).<br>Test odtwarza obie ścieżki na atrapie (`tryb: "odmowa"` i `tryb: "blad"`) i sprawdza, że komunikaty się **nie mieszają**. Sprawdzone mutacją. |
| **D28** | Uzupełnianie kalendarza idzie **przed** rysowaniem, a siatka bezpieczeństwa liczy slot tak samo jak zapis (naprawa **B23**) | Rysowanie jest kosmetyką — dawka w kalendarzu nie jest, i kolejność w kodzie ma to odzwierciedlać. We wszystkich czterech ścieżkach (odświeżanie, nasłuch zdarzeń, nasłuch kalendarza, nasłuch ustawień) `doReconcile()` stoi teraz jako pierwsze. Wyjątek w renderze nie ma już jak zabrać ze sobą zapisu dawki.<br>`brakujePokrycia()` czyta `doses[dzień][0]` — dokładnie to miejsce, w które pisze `doReconcile()`. **Dwie funkcje opisujące ten sam fakt muszą liczyć go tak samo**; rozjazd nie dawał błędu widocznego z zewnątrz, tylko cichy pusty przebieg co minutę.<br>**Morał, drugi raz tego dnia po D26:** test sprawdzający *literalny kształt kodu* zamiast *zachowania* potrafi utrwalić błąd zamiast go złapać — tutaj wprost wymagał, żeby uzupełnianie stało na końcu, za rysowaniem. Nowe testy pilnują kolejności **i** zachowania, obie sprawdzone mutacją.<br>Nowy test klucza dnia liczy go przez `devKey(ts)`, a nie `todayKey()` — inaczej padałby losowo o 01:xx w części stref (ta sama pułapka co **N3**). |
| **D25** | Aplikacja odróżnia **„nie ma zasięgu"** od **„kolejka stoi mimo połączenia"**, a pudełko melduje, ile zostało mu pamięci | Powstało z **B20**. Kuba wrócił z wyjazdu, pudełko zameldowało się z domu — a aplikacja napisała o dziesięciu zaległych zdarzeniach tym samym spokojnym, żółtym zdaniem, którego używa przy chwilowym braku zasięgu: *„nic nie zginęło, dośle się samo"*. Było dokładnie odwrotnie.<br>**Rozróżnienie ma twardą podstawę, a nie jest domysłem:** pudełko wysyła status **po** próbie opróżnienia kolejki, więc świeży `lastSeen` z niezerowym `queued` to zapis z chwili, w której połączenie było, a mimo to nic nie poszło. `ZATKANA_SWIEZOSC_H` = 6 h — świadomie **więcej** niż maksymalny backoff pudełka (`RETRY_MAX_S` = 4 h), żeby nie krzyczeć na pudełko, które po prostu jeszcze nie spróbowało — i **mniej** niż `MILCZY_PROG_H` = 26 h, żeby dwa ostrzeżenia nie mówiły naraz dwóch sprzecznych rzeczy.<br>**Do tego pomiar, zamiast trzeciej hipotezy z rzędu.** `nvsWolneWpisy()` (`nvs_get_stats`) idzie w statusie jako `nvsFree`. Cała pamięć trwała pudełka to jedna partycja ~20 kB na **wszystko**: kolejkę zdarzeń, dziennik wieczka, czarną skrzynkę i token. Gdy się zapełni, każdy z tych mechanizmów zaczyna gubić wpisy po cichu. `nvsFail` mówi, że to **już** się dzieje; `nvsFree` mówi, że **zaraz** zacznie — próg 40 wolnych wpisów z ~640 to dzień, może dwa zapasu.<br>Brak pola traktujemy jako **brak pomiaru**, nie jako zero: starszy firmware nie przysyła `nvsFree` w ogóle i nie ma krzyczeć o pamięci, której nikt nie zmierzył.<br>Przy okazji naprawione **B19** (backoff nie wracał do normy po powrocie sieci). Odmowa na stałe (HTTP 400) **też** zeruje backoff — baza odpowiedziała, czyli sieć działa; awaria serwera i brak sieci go **nie** zerują, bo dobijanie się co 15 minut do martwej bazy zjadłoby ogniwo.<br>**Morał, po raz kolejny w tym projekcie: różnica między „czekam" a „stoję" jest niewidoczna, dopóki ktoś jej wprost nie nazwie.** |
| **D16** | Odmowa bazy przesuwa nas do następnego wpisu, zamiast przerywać kolejkę (naprawa **B6**) | `oczekWyslij()` przerywał na pierwszym niepowodzeniu — słusznie, gdy sieci nie ma, bo każda próba to osiem sekund. Ale gdy baza **odpowiadała i odmawiała** jednego wpisu, ten wpis blokował wszystkie dawki za sobą, na zawsze. To ta sama rodzina co **B3**.<br>`bazaOdmowila()` rozróżnia teraz „baza odmówiła" od „bazy nie ma" — dokładnie to samo rozróżnienie, które po stronie pudełka robi `trwaleOdrzucony()`. Brak kontaktu dalej przerywa pętlę; odmowa pojedynczego wpisu tylko go omija.<br>**Świadomie NIE kasujemy odrzuconego wpisu** — i tu różnimy się od `queueDrop()` z D13. Tam kasowanie jest konieczne, bo kolejka ma 120 miejsc i nadpisuje najstarsze. Tu kolejka siedzi w telefonie, ma sufit 200 i nikomu nie przeszkadza, a `PERMISSION_DENIED` dostaniemy także przy **wygasłej sesji** — czyli w stanie przejściowym. Kasowanie na taki domysł łamałoby zasadę 6 bez potrzeby.<br>Odmowy są liczone i pokazywane osobno od „czeka na połączenie": napisanie „aplikacja dośle je sama" o odmowie byłoby po prostu nieprawdą. |
| **D13** | `queueDrop()` — jeden świadomy wyjątek od „nic przed 2xx" (naprawa **B3**) | Zasada chroni przed utratą danych przy **chwilowej** awarii. Wpis odrzucony regułami (HTTP 400) albo z uszkodzoną treścią nie zostanie przyjęty ani teraz, ani za tydzień — a zostawiony na czele blokuje **wszystkie dawki za sobą**. Tracimy jeden wpis zamiast całej kolejki.<br>Które kody są „na stałe": **400** i **413**. Świadomie **nie ma** tam 401/403 (wygasły token — po odświeżeniu przejdzie) ani **404** (zły adres bazy skasowałby całą kolejkę). Test pilnuje każdego z tych przypadków osobno.<br>Strata idzie na licznik `dropped` w statusie — kasujemy głośno, nie po cichu. |
| **D12** | Licznik tabletek liczy **od bazy**, nie różnicowo (naprawa **B4**) | Nowe pola w `config`: `pillsBase` (stan na początek dnia `pillsBaseFrom`) i `pillsBaseFrom`. `pillsLeft` zostaje, ale jest już **wynikiem**, nie licznikiem: baza minus wszystko wzięte od tej daty, przeliczane od zera przy każdej zmianie. Dzięki temu kolejność przestaje mieć znaczenie — dzień dosłany wstecz też wchodzi do sumy.<br>`pillsCountedUntil` **zostaje w bazie i dalej idzie do przodu**, choć nowy kod go nie używa: telefon z zakeszowaną starą wersją liczy po staremu i bez tego odjąłby wszystko drugi raz.<br>Ręczny wpis („ile tabletek jest w opakowaniu") ustawia bazę **na dziś** — to policzone tabletki leżące teraz, więc dni zamknięte wcześniej są już w tej liczbie i pudełko dosyłając je później nie może odjąć drugi raz.<br>Migracja jednorazowa i automatyczna: `pillsBase = pillsLeft`, `pillsBaseFrom` = dzień po znaczniku. Stan licznika się nie zmienia. Reguły: `config` ma `$other: true`, więc **nie trzeba publikować nowych** (walidacje dopisane na przyszłość). Firmware bez zmian — czyta z `config` tylko `pillsLeft`, żeby piknąć „mało tabletek". |
| **D6** | Znaczniki „to ja" w `users/<uid>/lidMarks`, nie przy zdarzeniu | `events` ma w regułach `$other: false` — nieznane pole = odrzucenie **całego** zdarzenia. Ugryzło już raz, przy `openTs` |
| **D2** | Kasujemy z pamięci pudełka dopiero po potwierdzonym 2xx | Jedna nieudana wysyłka niszczy **jedyną** kopię danych. To rodzina błędu 3.5 |
| **D1** | Zapis „to ja" idzie przez `zapiszPewnie()` | Firebase offline nie odrzuca obietnicy, tylko wisi w nieskończoność. Ekran pokazuje ptaszka, dane nie docierają |
