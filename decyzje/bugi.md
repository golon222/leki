# Błędy zamknięte — historia objawów

Otwarte błędy stoją w `DECYZJE.md`. Tu jest to, co **już naprawione**: objaw, przyczyna i numer decyzji, która to załatwiła. Czyta się to wtedy, gdy podobny objaw wraca — nie na wejściu do zadania.

| # | Bug | Objaw | Czemu jeszcze stoi |
|---|---|---|---|
| **B29** | ~~Reczny wpis znikal po odswiezeniu, mimo ze w bazie byl poprawny~~ **naprawione — D35** | Kuba dal precyzyjny przebieg i twardy dowod: wpisał 8 sierpnia ręcznie, działało, mógł odświeżać wielokrotnie — wszystko OK. Twardy restart aplikacji: **od razu po otwarciu wciąż widać poprawny wpis.** Dopiero po kolejnym odświeżeniu dawka znika — a **Firebase Console w tym samym momencie pokazuje poprawny ręczny wpis w bazie.**<br>To rozstrzygnęło sprawę ostatecznie: to nigdy nie był zapis nadpisany. To był **zły odczyt** po stronie telefonu — `doReconcile()` liczył decyzję na lokalnej kopii `doses`, która w chwili liczenia bywała nieaktualna. Przyczyn tej nieaktualności znalazłem po kolei trzy: kolejkę offline (**B27**), brak `await` na starcie (**B28**, pierwsza runda), wyścig dwóch nasłuchów po `goOnline()` (**B28**, druga runda) — i za każdym razem naprawa zamykała jedną konkretną ścieżkę do tego samego objawu, nie samą przyczynę. |
| **B28** | ~~Zaległa kolejka nigdy nie dostawała szansy na starcie aplikacji~~ **naprawione — D34, druga runda** | Kuba trafił diagnozą lepiej niż ja: *„aplikacja nie zmienia danych w bazie, przez co po wejściu do aplikacji ponownie zczytuje mi dane z bazy gdzie nie jest to zaktualizowane"*. Moja poprzednia naprawa (**B27/D33**) była realna, ale odpowiadała na inne pytanie — chroniła istniejący wpis przed nadpisaniem. Nie pomagała, jeśli wpis **w ogóle nie dotarł** do bazy.<br>**Pierwsza runda tej naprawy (poniżej) miała lukę, którą znalazłem dopiero dzięki surowemu odczytowi z konsoli Firebase od Kuby** — `users/.../doses/2026-08-08/0` pokazywało `source:"device"` ze świeżym `ts`, mimo ręcznego wpisu chwilę wcześniej. To był twardy dowód, nie kolejna hipoteza.<br>**Prawdziwa przyczyna: `oczekWyslij()` w `boot()` był wywołany BEZ `await`.** Funkcja startowała, ale `boot()` leciał dalej i rejestrował nasłuchy `onValue` **natychmiast** — zanim zaległy zapis zdążył dotrzeć do serwera. Nasłuch zdarzeń widział jeszcze dobę bez wpisu, sam uruchamiał uzupełnianie, a to pisało świeże `missed`, które ścigało się o ten sam wpis z Twoim zapisem i czasem wygrywało. Ten sam błąd siedział w drugim miejscu — przy powrocie łączności, gdzie `wakeUp()` startował zaraz obok `oczekWyslij()`, zamiast po nim.<br>**Zgłoszenie Kuby dokładnie odtwarzało tę lukę:** twardy restart aplikacji (usunięcie z przełącznika zadań na iOS) to świeże `boot()` od zera — dokładnie ścieżka, na której wyścig był możliwy.<br>Test na samo *wystąpienie* wywołania (bez sprawdzenia `await`) przechodził na obu wersjach — poprawionej i błędnej — bo `oczekWyslij()` wołane jest w obu. Test musiał zacząć szukać dosłownie `await oczekWyslij()`. |
| **B27** | ~~Ręczny wpis „cofał się" po wyjściu z aplikacji~~ **naprawione — D32** | Zgłoszone przez Kubę: *„wpisuję ręcznie tego 8, a po wyjściu z aplikacji mi się to cofa do tego że nie wziąłem"*.<br>**Sprawdziłem najpierw, czy baza nie odrzuca tego zapisu — nie odrzuca.** Payload ręcznej korekty przechodzi przez prawdziwe `database.rules.json` (uruchomiłem go przez `rules_engine`). Więc to nie była odmowa, tylko nadpisanie.<br>`doReconcile()` chronił ręczną korektę warunkiem `cur.source === "manual"`, ale `cur` czyta **lokalną kopię** `doses`. Gałąź „pominięte" miała `if (cur && cur.status === "taken") continue` — czyli nadpisywała **wszystko, co nie było „wzięte"**. Wystarczy, że lokalna kopia jest o chwilę nieaktualna — a `wakeUp()` przeładowuje ją **dokładnie przy powrocie do aplikacji** — i ochrona nie ma czego sprawdzić. Trzy zdarzenia „pominięte" z 8.08 czekały gotowe, żeby wjechać na miejsce ręcznego wpisu.<br>**Ochrona oparta na tym, że lokalna kopia jest świeża, nie jest ochroną** — jest wyścigiem, który akurat zwykle wygrywamy. |
| **B26** | ~~Pasek nawigacji „rozjeżdżał się przy scrollowaniu" na iPhonie~~ **Kuba potwierdza: w porządku** (2026-08-11) | Zgłoszone przez Kubę. Trzy rzeczy naraz: (1) **nigdzie nie było prefiksu `-webkit-backdrop-filter`**, którego iOS Safari wymaga do rozmycia tła; (2) pasek `position:fixed` z rozmyciem tła nie miał własnej warstwy kompozycji, więc jego tło potrafi zostawać w tyle za przewijaniem; (3) `body` **i** `nav` rezerwowały miejsce na wcięcie ekranu — a dla elementu `fixed` wypełnienie `body` nic nie daje, tylko dokłada wysokości pod treścią, przez co przy odbiciu (rubber-band) pasek odrywał się od dołu. | Naprawa **nigdy nie została jawnie zweryfikowana pomiarem** — nie mam iPhone'a i nie potrafię odtworzyć renderowania iOS. Kuba potwierdził ustnie, że nie zgłasza dalej problemu — „jakby było nie tak, to bym cię męczył od razu". Traktuję to jako wystarczające potwierdzenie w praktyce, mimo że nie jest to zrzut ekranu ani pomiar. |
| **B25** | ~~Aplikacja widziała JEDNO zdarzenie zamiast czterdziestu dwóch~~ **naprawione — D30** | Kuba przysłał zrzut Diagnostyki, na którym stały **obok siebie, w tej samej chwili**: „Pokazano **1** z 1 zdarzeń" i „✓ Zdarzeń z pudełka: **42**". To samo zapytanie, ten sam użytkownik, ta sama baza.<br>Różniły się **jednym znakiem — klamrą**:<br>`s.forEach(c => { evs.push(...); });` → działa, zwraca `undefined`<br>`s.forEach(c => events.push(...))` → zwraca `push()`, czyli **długość tablicy**<br>**`DataSnapshot.forEach` przerywa enumerację, gdy callback zwróci coś prawdziwego** (SDK robi dosłownie `!!action(...)`). `push()` zwraca liczbę ≥ 1 — czyli **zawsze**. Pętla kończyła się po pierwszym dziecku, zawsze, w obu miejscach, gdzie aplikacja czyta zdarzenia.<br>**To jest właściwa przyczyna całej sprawy z dniami 6–9 sierpnia.** Uzupełnianie kalendarza dostawało jedno zdarzenie i nie miało z czego ich odtworzyć — niezależnie od tego, ile ich naprawdę było w bazie. Wszystkie moje wcześniejsze hipotezy (kontaktron, „missed", zatkana kolejka) opisywały pudełko, a błąd siedział w telefonie. |
| **B24** | ~~Pudełko potwierdzało dźwiękiem dawkę, której NIE ZAPISAŁO~~ **naprawione — D29** | Zgłoszone przez Kubę jednym zdaniem, które rozstrzygnęło sprawę: *„na milion procent za każdym razem był charakterystyczny dźwięk, jakby próbował wysłać, ale mu się nie udało"*.<br>Ten dźwięk to `beepQueued()` i gra **wyłącznie** na jednej ścieżce: `reportEvent()` już się wykonał, wysyłka nie przeszła, zdarzenie poszło do kolejki. Czyli **kontaktron działał za każdym razem** — moja diagnoza „pudełko nie zarejestrowało otwarć" była błędna i Kuba ją obalił jednym faktem z terenu.<br>Ale ten dźwięk mówił jeszcze coś. `queuePush(rec);` — **wynik ignorowany**. Ta funkcja od naprawy **B5** zwraca `false`, gdy zapis do NVS nie przeszedł: treść nie zostaje wtedy **nigdzie**, tylko licznik się nie podnosi. Pudełko meldowało więc „mam to u siebie, wyślę później" o dawce, której nie miało. Log `[EVT] zapisane w pamieci` kłamał tak samo.<br>**To najgorszy rodzaj kłamstwa w tym urządzeniu:** sygnał dźwiękowy jest jedyną rzeczą, jaką pudełko mówi bez internetu. Człowiek słyszy potwierdzenie, zamyka wieczko i jest spokojny. |
| **B23** | ~~Kalendarz uzupełniał się dopiero po ręcznym kliknięciu~~ **naprawione — D28** | Zgłoszone przez Kubę: *„dzisiejszy dzień się zsynchronizował po naciśnięciu «przelicz kalendarz ze zdarzeń», a to się samo powinno robić też"*. Miał rację — automat był podpięty w czterech miejscach i mimo to nie działał.<br>**Dwie osobne przyczyny.**<br>**(1) `doReconcile()` stało ZA rysowaniem.** W `wakeUp()` — sześć wywołań rysujących, dopiero potem uzupełnianie, całość w jednym `try`. Wyjątek w którymkolwiek renderze (brakujący element, pole, którego starszy firmware nie przysyła) leciał do `catch` i **dawka nie trafiała do kalendarza**. To ten sam kształt błędu, który komentarz dwie linijki wyżej już opisuje: naprawiono wtedy tylko odczyty (`allSettled`), a `doReconcile` zostało na końcu. Ta sama pułapka w trzech nasłuchach.<br>**(2) Siatka bezpieczeństwa patrzyła na inny slot, niż zapisuje uzupełnianie.** `brakujePokrycia()` czytało `doses[dzień][ev.slot]`, a `doReconcile()` od naprawy **B9** pisze **zawsze do slotu 0**. Przy dwóch przypomnieniach zdarzenie ze `slot: 1` nie miało pokrycia nigdy — kontrola co minutę wołała uzupełnianie bez końca i nie potrafiła zameldować „pokryte".<br>**Test utrwalał błąd:** sprawdzał wprost, że `doReconcile` stoi *na końcu* odświeżania. Zielony, i pilnujący dokładnie tej kruchości, przez którą dawka ginęła. |
| **B22** | ~~Czarna skrzynka nie zapisała ANI JEDNEJ linijki~~ **naprawione — D27** | Zgłoszone przez Kubę zaraz po wgraniu 1.28.1: „Pudełko zgłasza utratę danych — 3 zapisów się nie udało", a **Historia pudełka pusta**.<br>`Preferences` to **jeden globalny obiekt z jednym uchwytem**: `begin()` na już otwartym zwraca `false` i nic nie robi, ale `end()` tego zagnieżdżonego wywołania **zamyka uchwyt funkcji nadrzędnej**. `logbookAdd()` otwierał `prefs`, po czym w argumentach `snprintf` wołał `queueCount()` — które samo robi `begin()`/`end()`. Po powrocie uchwyt był zamknięty, więc `nvsPutStr()` zawodził **za każdym razem, przy każdym wybudzeniu**.<br>Skutek podwójny: dziennik zawsze pusty, a `pushStatus()` nadpisywał historię w bazie pustą tablicą `[]` — więc ginęła też ta zapisana wcześniej.<br>**Błąd istniał od dawna, także w 1.22.0 — tylko był CICHY**, bo przed D14 nikt nie patrzył na wynik `putString()`. To jest dokładnie to, po co powstało D14: zamieniło ciche znikanie danych w widoczny licznik, i pierwszą rzeczą, którą pokazało, był ten błąd. |
| **B21** | ~~Żadna wersja firmware od 1.23.0 nie dawała się wgrać z Arduino IDE~~ **naprawione — D26** | Zgłoszone przez Kubę: `error: 'WakeReason' was not declared in this scope`, linia 544 — w miejscu wyglądającym na zupełnie poprawne.<br>**Arduino IDE dopisuje prototypy wszystkich funkcji i wstawia je TUŻ PRZED PIERWSZĄ DEFINICJĄ FUNKCJI w pliku.** Naprawa **B5** (D14) dołożyła `nvsPutStr()` na samej górze i przesunęła ten punkt **ponad** `enum WakeReason` — wygenerowany prototyp `const char* wakeName(WakeReason w);` odwoływał się do typu, którego kompilator jeszcze nie znał.<br>Dowód liczbowy, nie hipoteza: 1.22.0 ma pierwszą funkcję w linii 134, a enum w 100 → kompiluje się. 1.27.0 ma pierwszą funkcję w 109, a enum w 130 → pada. Odtworzone lokalnie co do numeru linii i kolumny.<br>**To dlatego pudełko Kuby stało na 1.22.0.** Nie dlatego, że nie próbował — dlatego, że się nie dało. Przez ponad miesiąc wszystkie naprawy (B3, B5, B9–B17, B19) leżały w repo i **nie miały jak trafić na płytkę**, a `kompiluj_firmware.sh` przez cały ten czas meldował sukces. |
| **B19** | ~~Backoff nie wracał do normy po powrocie sieci~~ **naprawione — D25** | `rtcRetryCount` zerował się **wyłącznie** w `reportEvent()`, czyli tylko przy otwarciu pudełka. Po kilku dobach bez internetu licznik stał na maksimum, a przerwa między próbami wynosi wtedy **cztery godziny**. Wybudzenia rutynowe (`fetchConfig(); flushQueue(); pushStatus();`) nie ruszały go wcale — więc powrót do domu niczego nie przyspieszał: jeśli zaległości nie zmieściły się w jednym czuwaniu (`AWAKE_LIMIT_MS`), reszta czekała kolejne cztery godziny mimo sprawnej sieci. | — |
| **B17** | ~~Alarm dzwonił w kółko przez trzy godziny~~ **naprawione — D23** | Zgłoszone przez Kubę **z wyjazdu**: *„pika już 3 raz od 18:30, o 19:30 też, a tabletkę już wziąłem"*. Po wyczerpaniu `MAX_ALARM_RETRIES` alarm zapisywał „missed" i czyścił `rtcPendingSlot`, ale **nie zostawiał śladu, że dla tej doby już dzwonił**. `matchSlot()` dopasowuje porę leku w oknie ±90 min, więc dla 20:00 zwraca slot 0 przez **całe trzy godziny** (18:30–21:30) — a każde wybudzenie w tym oknie zaczynało alarm od nowa.<br>**Widoczne wyłącznie offline:** z siecią kolejka jest pusta, więc nie ma po co budzić pudełka w środku okna. Bez sieci `planNextSleep()` budzi je do ponawiania wysyłki po 15, 30, 60, 120 minutach — i każde takie wybudzenie odpalało pełny alarm. Stąd 18:30, potem 19:30. | — |
| **B16** | ~~Bez internetu NIE BYŁO ochrony przed drugą dawką~~ **naprawione — D22** | Zgłoszone przez Kubę **z wyjazdu**, na prawdziwym urządzeniu: wziął tabletkę, zamknął wieczko, otworzył ponownie — i cisza, żadnego „już dziś brałeś". Cała ochrona była bramkowana `rtcTimeValid`, a tę flagę ustawia **wyłącznie** `syncTimeNTP()`. Po resecie (wgranie programu przed wyjazdem, zanik zasilania) i bez sieci pudełko ani nie **zapisywało** wziętej dawki, ani nie miało na czym oprzeć ostrzeżenia. Sześć osobnych miejsc, wszystkie za tym samym warunkiem. | — |
| **B9** | ~~Dawka wzięta poza godziną przypomnienia przepadała~~ **naprawione — D19** | `cfg.schedule` to godziny **przypomnień**, nie pory brania leku. Przy jednym przypomnieniu `doReconcile()` miał furtkę „jedna pora → slot 0" i wszystko działało. Przy **dwóch** furtka znikała: otwarcie o 14:00 nie mieściło się w oknie ±90 min żadnego z nich, `slot` zostawał −1, a `continue` wyrzucało zdarzenie do kosza. Dawka nie trafiała do kalendarza i wyglądała jak pominięta. Do tego `target` i `perDay` liczyły `defaultDose × schedule.length`, więc każdy poprawny dzień robił się „częściowy", a prognoza zapasu malała o połowę. | — |
| **B10** | ~~Pusta godzina przypomnienia rozjeżdżała numerację~~ **naprawione — D19** | `saveConfig` nie sprawdzał formatu. Wyczyszczone pole czasu dawało `["", "20:00"]`; reguły to przepuszczały, a `parseSchedule()` odrzucał pustą pozycję — więc pudełko liczyło przypomnienia od innego numeru niż aplikacja. | — |
| **B11** | ~~Data pomiaru INR z przyszłości wyciszała przypomnienie~~ **naprawione — D19** | `addInr` sprawdzał wartość, ale nie datę. Literówka w roku sprawiała, że `inrTerminKey()` brał ten wpis jako ostatni pomiar i termin skakał o rok. Bez słowa. | — |
| **B12** | ~~Jeden brakujący plik zabijał aktualizacje aplikacji~~ **naprawione — D19** | `sw.js` używał `caches.addAll(SHELL)`, a to jest atomowe: 404 na którymkolwiek pliku wywracał instalację, `skipWaiting()` nigdy się nie wykonywał i worker nie przejmował kontroli. Razem z nim znikał **cały** mechanizm aktualizacji — telefon zostawał na starej wersji i nic o tym nie mówił. | — |
| **B13** | ~~Zaległe doby po dłuższej przerwie ginęły~~ **naprawione — D19** | `checkDayRollover()` tworzył **jeden** wpis „missed" ze znacznikiem wczoraj 23:59 i przestawiał `rtcRolloverDay` od razu na dziś. Po kilku dniach bez zasilania dni pośrednie nie dostawały nic — w kalendarzu wyglądały jak „brak danych", czyli odwrotnie niż w rzeczywistości. | — |
| **B15** | ~~Zakres terapeutyczny INR bez granic~~ **naprawione — D20** | Pola „Od" i „Do" przyjmowały dowolną liczbę. Zakres w rodzaju 0,2–9 sprawiał, że ocena „w zakresie / poza zakresem" przestawała cokolwiek znaczyć, a odwrócony (3–2) dawał przedział pusty i pasek o ujemnej wysokości na wykresie. | — |
| **B14** | ~~Drobne dziury w walidacji wejścia~~ **naprawione — D19** | Pusty prompt zerował licznik tabletek (`+"" === 0`); `defaultDose` bez górnego limitu powodował odrzucenie **całego** zapisu ustawień z komunikatem „czeka na połączenie"; granice INR rozjeżdżały się między polem (12), kodem (15) i regułami (15). | — |
| **B6** | ~~Trwale odrzucony zapis blokuje kolejkę w telefonie~~ **naprawione — D16** | `oczekWyslij()` przerywał na pierwszym niepowodzeniu. Wpis, którego reguły nie przyjmą (nieznane pole), stał na czele i blokował wszystkie dawki za sobą — rodzina **B3**. | — |
| **B8** | ~~Wygasły token zatyka kolejkę bez śladu~~ **to nie był błąd — patrz niżej** | — | — |
| **B2** | ~~Autotest (3 kliknięcia) nie dochodzi do końca~~ **Kuba melduje: działa** (2026-08-11, na firmware 1.29.0) | Kuba: *„czekało na synchronizację, a jak już była, to nic się nie pojawiało"*. Nie wiadomo było, czy winne zasypianie, czy co innego — **przyczyna nadal nie jest zdiagnozowana ani opisana żadną naprawą w tej sesji**. | Test terenowy potwierdził, że autotest teraz kończy się poprawnie. Zostaje jako **obserwacja, nie zamknięty przypadek**: skoro nie wiemy, co konkretnie to naprawiło (któraś z napraw firmware 1.23.0–1.29.0 ubocznie, albo błąd był rzadki i akurat się nie powtórzył), nie ma pewności, że nie wróci. Trzymamy otwarte, dopóki nie powtórzy się kilka razy bez objawu. |
| **B5** | ~~Wynik zapisu do pamięci trwałej (NVS) nigdy nie sprawdzany~~ **naprawione — D14** | `prefs.putString()` zwraca liczbę zapisanych bajtów — `0` gdy się nie udało. W siedmiu miejscach wyrzucaliśmy tę wartość do kosza, więc nieudany zapis wyglądał dokładnie jak udany. W kolejce oznaczało to **cichą utratę dawki**. | — |
| **B4** | ~~Dzień dosłany wstecz nie odejmował tabletki~~ **naprawione — D12** | Licznik liczył różnicowo i pomijał dni starsze od znacznika `pillsCountedUntil`. Pudełko offline od wtorku, w czwartek dopisujesz ręcznie środę → znacznik skacze na środę → wtorek dosłany w piątek nie odejmie się **nigdy**. Licznik rósł ponad stan faktyczny. | — |
| **B3** | ~~Odrzucony wpis zatyka kolejkę dawek na zawsze~~ **naprawione — D13** | `flushQueue()` przy niepowodzeniu wychodził **nie zdejmując wpisu z kolejki**. Wpis odrzucony przez reguły (HTTP 400) nigdy nie zostanie przyjęty, więc blokował wszystko za sobą — dawki piętrzyły się, aż pierścień 120 wpisów zaczynał nadpisywać najstarsze. | — |

---

## Zgłoszone jako błąd, a błędem nie są

> Wiedza „sprawdzone, nie ruszać" jest warta tyle samo co lista usterek —
> chroni przed naprawianiem tego samego drugi raz.

| # | Co zgłoszono | Dlaczego to nie jest błąd |
|---|---|---|
| **B7** | Próg „mało tabletek" znaczy co innego w pudełku (7 **tabletek**) niż w aplikacji (7 **dni zapasu**) | **Zamknięte decyzją Kuby, 2026-08-07.** Rozjazd istnieje wyłącznie przy więcej niż jednej dawce dziennie, a tego przypadku **nie będzie** — Warfin idzie raz dziennie i nie ma scenariusza, w którym się to zmienia. Przy jednej dawce obie liczby są tożsame. Test zgodności pilnuje progów po obu stronach, więc gdyby ktoś kiedyś ruszył `LOW_STOCK_WARN` albo próg w aplikacji, dowie się o tym od razu. |
| **B8** | Wygasły token miał zatykać kolejkę bez śladu w licznikach | **Mój błąd w analizie — przeczytałem ścieżkę za pobieżnie.** `rtdbSend()` przy 401/403 woła `zapomnijToken()`, a to zeruje `rtcTokenExp` **i** kasuje kopię z NVS. Najbliższe `firebaseSignIn()` nie ma już czemu ufać, więc loguje się hasłem od nowa. Cała szkoda to **jedno spalone zapytanie**, nie zatkana kolejka. Założenie stojące za **D13** („401 to wygasły token, po odświeżeniu przejdzie") jest więc prawdziwe. Zostaje test, bo pilnuje drugiej połowy tej umowy: dopóki token nie wróci, nic nie ma prawa zginąć. |
