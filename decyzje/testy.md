# Decyzje — Testy, audyt, kompilacja

tests/: atrapy, kontrole statyczne, toolchain Arduino

Indeks wszystkich decyzji: `DECYZJE.md`. Tu są **pełne** wpisy z tego obszaru (11 szt.), od najnowszego.

| # | Decyzja | Co by było bez tego |
|---|---|---|
| **D86** | **Dziennik decyzji rozbity na indeks + obszary, duże pliki dostają generowaną mapę** (narzędzia pracy, nie kod) | Pytanie Kuby: *„czy da się jakoś zoptymalizować ten projekt, żeby był token friendly"*. Zmierzone, zanim cokolwiek ruszyłem: `DECYZJE.md` **60 tys. tokenów**, `index.html` 130 tys., `PillBox.ino` 89 tys., `CLAUDE.md` 6 tys.<br>**Sedno: płaciliśmy pełną cenę za wiedzę, z której w danym zadaniu potrzeba 2–3 wpisów.** `CLAUDE.md` kazał czytać cały dziennik przed każdą zmianą — słusznie co do zasady, bo nieznajomość powodu poprawki kosztowała w tym projekcie całe wieczory (B21, D48–D52). Ale „przeczytaj wszystko" i „nie cofaj poprawki, nie znając powodu" to nie to samo zdanie.<br>**Podział:** indeks jednolinijkowy w `DECYZJE.md` (88 wpisów: numer, o co chodziło, plik) plus pełne wpisy w `decyzje/` po obszarach — `pudelko`, `ota`, `telegram`, `aplikacja`, `dane`, `testy`, `bugi`, `cofniete`. Wejście w zadanie: **5,7 tys. zamiast 60 tys.**, plus jeden plik obszaru. Ani jeden znak treści nie zniknął — podział zrobił skrypt, a sprawdzenie porównało zbiory wierszy przed i po.<br>**Błędy otwarte zostają w indeksie w całości** (B20, B1, B18, N3). To jedyna część dziennika, której nieznajomość prowadzi do zepsucia czegoś dziś — B1 wprost mówi „nie ruszaj z własnej inicjatywy".<br>**`MAPA.md`:** spis treści `index.html` i `PillBox.ino` z numerami linii, generowany przez `tests/mapa.py` przy **każdym** przebiegu testów. Zamiast czytać 7853 linie, żeby znaleźć rekonsyliację, czyta się `sed -n '2784,2980p'`. Mapa nieaktualna byłaby gorsza niż żadna — dlatego jest generowana, nie pisana.<br>**Czego to NIE zmienia:** runner nadal wypluwa cały log przy błędzie (D66), a `CLAUDE.md` nie stracił ani jednej reguły — wypadły tylko powtórzenia stojące już w `decyzje/`. Sprawdziłem to porównaniem wszystkich identyfikatorów w backtickach przed i po.<br>**Nowa kontrola statyczna** (25. z kolei) pilnuje, że indeks i pliki się zgadzają: wpis bez linijki w indeksie jest niewidoczny, linijka bez wpisu prowadzi donikąd. Sprawdzona mutacją w obie strony. |
| **D67a** | **`strip()` w audycie psuł się na `//` wewnątrz literału — i przez to audyt sprawdzał tekst, którego w kodzie nie ma** | Wzorce działały osobno, a język czyta się po kolei. Adres w napisie — `"https://"` — sprawiał, że wzorzec kasujący komentarze `//` zjadał resztę linii **razem z zamykającym cudzysłowem**; od tego miejsca kolejny wzorzec liczył napisy z przesunięciem o jeden i kasował kod.<br>Przy trzech takich adresach w pliku wychodziło na zero i **nikt tego nie zauważył przez cały czas istnienia audytu**. Czwarty (adres Telegrama) urwał **32 kB źródła**, w tym cały `switch (wakeReason)` — i dopiero wtedy audyt się wysypał.<br>**To jest dokładnie ta sama wpadka, którą ta funkcja miała już raz** (apostrof w HTML-u portalu). Tamten raz objawił się ciszą, ten wyjątkiem. Cichy wariant jest groźniejszy, więc zamiast lepić trzeci wzorzec przeszedłem na **skanowanie znak po znaku**, które nie ma jak się rozjechać.<br>**Po naprawie audyt wykonuje 296 kontroli zamiast 276** — te dwadzieścia leżało w zjedzonym kawałku i nie sprawdzało niczego. |
| **D67b** | **Atrapa NVS nie umiała zgubić danych po cichu — czyli nie umiała odtworzyć jedynej awarii, przed którą broni odczyt kontrolny** | `hasloUtrwal()` (**D59**) i `tgUtrwal()` czytają zapis z powrotem, bo NVS w tym urządzeniu potrafi zawieść **cicho** (D46, D47). Atrapa umiała zawieść wyłącznie **głośno**: `failKeys` sprawia, że sam zapis zwraca zero.<br>Skutek: mutacja `return zapisC;` — czyli **usunięcie całego odczytu kontrolnego** — przechodziła przez cały zestaw testów. To samo dotyczyło hasła do bazy, od którego zależy zgoda na OTA, i było tak od D59.<br>Doszło `cichoGubKlucze`: zapis **melduje sukces**, a wartość nie zostaje. Obie mutacje są teraz łapane.<br>**Czwarty raz ta sama lekcja co w D30**, tym razem od strony, której wtedy nie zamknęliśmy: *atrapa, która nie umie zawieść dokładnie tak jak oryginał, mierzy inną rzecz niż się myśli*. |
| **D66** | **Zestaw testów cichy przy sukcesie, głośny przy błędzie** — 9700 → 185 tokenów na przebieg, bez utraty czegokolwiek przy awarii | Pytanie Kuby: *„dałoby się uprościć testy, żebyś przy testowaniu zjadał jak najmniej tokenów"*. Zasadne: pełny wypis to było **914 linii i 39 000 znaków**, z czego **533 linie to samo „OK" i nagłówki sekcji**. Czytam to kilka razy dziennie, więc płaciliśmy tokenami za informację „nic się nie stało" — najdroższą możliwą formę ciszy.<br>**Reguła, na której to stoi: oszczędzamy WYŁĄCZNIE na sukcesie.** Udany krok to jedna linijka z liczbą, którą ten krok sam o sobie podaje (`ZALICZONE: 484`), a nie zgadywaną z liczby linii. Krok, który zawiedzie, pokazuje **wszystkie** swoje linie błędu w całości — w tym projekcie każda z nich niesie plik, numer linii i nazwę kontroli, czyli komplet potrzebny do naprawy. Do tego ostatnie linie logu (tam siedzi podsumowanie i kontekst kompilatora) i **ścieżka do pełnego wypisu na dysku**, żeby nic nie ginęło bezpowrotnie.<br>**Gdy nic nie pasuje do wzorca błędu — pokazujemy wszystko.** Wysypka kompilatora albo crash nie mają linii `FAIL`; zgadywanie, co było ważne, kosztowałoby drugie uruchomienie „a pokaż szczegóły", czyli więcej, niż oszczędza. Sprawdzone na czterech rodzajach awarii: test firmware, test aplikacji, kontrola audytu i błąd kompilacji — ten ostatni wypadł najlepiej, bo `tail` sam złapał sygnaturę funkcji, źródło i daszek pod błędem.<br>**Ostrzeżeń kompilatora nie chowamy bez śladu**: udany krok pisze `(4 ostrzeżeń kompilatora)`. Cicha zamiana `warning` w nic to dokładnie ten rodzaj oszczędności, który kiedyś kosztuje wieczór.<br>**Przy okazji: `statyczna.py` wyjęte z runnera.** Kontrola statyczna była **wklejona w `run_all.sh` jako heredoc** — 118 linii Pythona, których nie dało się ani uruchomić osobno przy szukaniu jednej usterki, ani sprawdzić mutacją bez odpalania całego zestawu. Teraz jest zwykłym plikiem.<br>**Dla następnej sesji, wpisane wprost do `CLAUDE.md`:** te 12 linii **to jest** potwierdzenie, nie jego skrót. Powtórne uruchomienie z `SZCZEGOLY=1` „żeby sprawdzić dokładniej" kasuje całą oszczędność, a nie dowodzi niczego więcej — `✔` na końcu pojawia się wyłącznie wtedy, gdy wszystkie kroki wyszły. Przepuszczanie wyniku przez `grep` też nie ma już sensu; sam to robiłem odruchowo przez całą sesję, bo wypis był nie do przeczytania.<br>**Czego świadomie NIE zrobiono:** nie skrócono komunikatów o błędach, nie usunięto ani jednej kontroli i nie zmieniono tego, co testy sprawdzają. Pytanie brzmiało „mniej tokenów", nie „mniej testów" — a zestaw, który wykrywa mniej, oszczędza w miejscu, w którym oszczędzać nie wolno. |
| **D58** | `kompiluj_firmware.sh` podstawia **atrapę `ctags`**, gdy nie ma go w systemie | Świeży kontener nie ma `ctags`, a skrypt liczył na `command -v ctags` z odwołaniem awaryjnym do `/usr/bin/ctags`, którego też nie było. Builder przerywał z `fork/exec /usr/bin/ctags` **przed** kompilacją — czyli skrypt nie zdążył sprawdzić tego, po co w ogóle istnieje (B21: przez miesiąc firmware nie dawało się wgrać, a skrypt meldował sukces).<br>Atrapę wolno podstawić, bo **obie** ścieżki kompilacji podają arduino-cli gotowy `.cpp` i **pusty** `.ino` obok: generator prototypów dostaje pusty plik i nie ma z czego niczego wyciągać. Prototypy dla ścieżki „tak jak wgrywa Arduino IDE" robi `proto_arduino.py`, nie `ctags`. To ten sam chwyt co atrapa `dfu-util` z D17 i z tego samego powodu — brakujące narzędzie służy do czegoś, czego przy kompilacji nikt nie wykonuje.<br>Sprawdzone realnie: po zmianie **wszystkie cztery przejścia** przechodzą, `PillBox.ino` 38% (1 223 137 B), `PillBoxTest.ino` 12% — bajt w bajt tyle, co w dokumentacji. |
| **D45** | Test na wstrzyknięcie HTML sprawdzał **dwa duchy** (odpowiedź na pytanie Kuby „czy mamy jakieś niepotrzebne testy") | Pytanie było o testy niepotrzebne — znalazłem coś gorszego: test, który nic nie sprawdzał, ale wyglądał, jakby sprawdzał. `test_stress.mjs` miał tablicę id-ów do skanowania pod kątem wstrzykniętego `<img onerror>`: `["calGrid","todayWhen","hSub","inrList","boxLog","diagList","anaBody"]`. `diagList` zniknął z aplikacji w D43 (lista zdarzeń przeniosła się na własny ekran), a `anaBody` **nigdy nie istniał** — nic w kodzie nigdy nie renderowało do takiego id, żaden ekran go nie ma.<br>**Dlaczego to przechodziło bez ostrzeżenia:** `dom_stub.mjs` tworzy pusty element dla każdego nieznanego id (`getElementById` w prawdziwej przeglądarce zwróciłoby `null` i wywaliłoby wyjątek na `.innerHTML`). Pusty `innerHTML` nigdy nie zawiera `<img src=x`, więc `check(!caly.includes(...))` przechodził — nie dlatego, że ucieczka HTML działała, tylko dlatego, że dwa z siedmiu sprawdzanych miejsc były fikcją.<br>**Naprawa dwuczęściowa.** Po pierwsze: `diagList` zastąpiony przez `evLista` (następca — pełna lista zdarzeń ma teraz własny ekran), z prawdziwym wstrzykniętym zdarzeniem (`type: zly`) i wywołaniem filtrów na „wszystko", żeby nic go nie odfiltrowało. Po drugie, ważniejsze: doszła kontrola **przeciwko tej całej klasie błędu**, nie tylko przeciwko tym dwóm przypadkom — `idIstnieje(id)` czyta prawdziwy `index.html` i sprawdza, że każde testowane id tam **naprawdę jest**, zanim w ogóle spojrzymy na jego treść. Sprawdzone mutacją: przywrócenie `diagList`/`anaBody` do tablicy wywala dwie nowe kontrole.<br>**Czwarty raz w tym projekcie ta sama lekcja** (B22, D30, D39, teraz D45): *coś łagodniejsze od rzeczywistości mierzy inną rzecz, niż się myśli.* Tym razem to nie była atrapa Firebase ani NVS, tylko atrapa DOM — ale mechanizm identyczny: brak awarii tam, gdzie prawdziwe środowisko by ją dało, więc test milczy zamiast krzyczeć.<br>Poza tym jednym przypadkiem przegląd sześciu plików testów i `audit_firmware.py` (odwołania do nieistniejących id, zduplikowane nagłówki sekcji, funkcje sprawdzane w audycie, których nie ma już w `.ino`) nie znalazł nic więcej — reszta pokrycia jest aktualna. |
| **D30** | Atrapa Firebase odwzorowuje **przerywanie `forEach`**, a kontrola źródła zakazuje formy bez klamer (naprawa **B25**) | Atrapa ignorowała wynik callbacku, a `onValue` jest w niej pustą funkcją — więc kod nasłuchu **nigdy nie był w testach wykonany** i cała ta klasa błędów była niewidzialna. Teraz `forEach` przerywa się na każdej prawdziwej wartości, dokładnie jak `!!action(...)` w prawdziwym SDK.<br>Do tego kontrola statyczna: **żaden `forEach` na danych z bazy nie może zwracać wyniku `push()`**. Obie sprawdzone mutacją.<br>**Trzeci raz tego samego dnia: atrapa była łagodniejsza od rzeczywistości i przez to test przechodził na kodzie, który nie działa.** Wcześniej `Preferences.begin()` zawsze zwracało `true` z pustym `end()` (**B22**), a `putString()` nie meldowało niepowodzenia (**B5**). Wniosek na przyszłość: **atrapa, która nie umie zawieść dokładnie tak jak oryginał, mierzy inną rzecz niż się myśli.** |
| **D27** | Atrapa `Preferences` w testach zachowuje się jak **prawdziwe NVS**, a audyt zna pojęcie „funkcja otwierająca prefs" (naprawa **B22**) | Atrapa zwracała z `begin()` zawsze `true` i miała **puste `end()`** — więc cała ta klasa błędów była dla testów **niewidzialna**. Teraz modeluje `_started` tak jak rdzeń ESP32: zagnieżdżony `begin()` zwraca `false` i jest liczony, `end()` naprawdę zamyka, a przy `strict = true` zapis bez uchwytu zawodzi. Test odtwarza objaw Kuby co do liczby: dziennik `[]`, `nvsFail` rosnący.<br>Do tego kontrola statyczna w audycie: **żaden blok `prefs.begin()…end()` nie może wołać funkcji, która sama otwiera `prefs`** — sprawdzane **przechodnio** (A woła B, które woła C otwierające prefs) i po wycięciu komentarzy, bo nazwa funkcji w opisie nie jest jej wywołaniem.<br>Stara kontrola „brak zagnieżdżonych `prefs.begin()`" istniała i **przechodziła** — szukała dwóch literalnych `prefs.begin(` w jednej funkcji, a tu drugi siedział o jedno wywołanie dalej. **Morał: kontrola sprawdzająca literalny zapis zamiast zachowania przepuszcza dokładnie te przypadki, dla których powstała.**<br>Naprawa samego błędu to jedna linia: `queueCount()` wywołane **przed** `prefs.begin()`, wynik do zmiennej. |
| **D26** | Kompilacja sprawdza **obie** ścieżki: `.cpp` i tę, którą naprawdę wgrywa Arduino IDE (naprawa **B21**) | **D17 świadomie omijał generator prototypów** — kompilowaliśmy jako `.cpp`, bo łatana wersja `ctags` od Arduino jest niedostępna, a zwykły `exuberant-ctags` psuje prototypy `static`. Decyzja była poprawna co do problemu i **kosztowała miesiąc**: omijany etap to dokładnie ten, na którym wgrywanie padało.<br>**Morał, i to najdroższy w tym projekcie: test, który omija etap, nie odpowiada na pytanie, które się zadaje.** „Kod się buduje" i „Kuba to wgra" to dwie różne rzeczy, a przez miesiąc mierzyliśmy pierwszą, wierząc że mierzymy drugą.<br>Naprawa: `enum WakeReason` i `enum Gest` przeniesione **nad pierwszą definicję funkcji**. Robust niezależnie od tego, jak dokładnie działa generator — punkt wstawiania prototypów jest zawsze na pierwszej funkcji, więc typ zadeklarowany wyżej jest zawsze znany. (Wcześniejsza próba — ręczne deklaracje `wakeName`/`detectWakeReason` w linii 175 — celowała **pod** punkt wstawiania i nie mogła zadziałać; usunięta.)<br>Dwie niezależne blokady na przyszłość: `tests/proto_arduino.py` odtwarza wstawianie prototypów bez `ctags` (jest w `kompiluj_firmware.sh` jako osobny przebieg **obu** szkiców), a audyt sprawdza bez sieci, czy każdy typ własny użyty w sygnaturze stoi przed pierwszą funkcją. Obie sprawdzone mutacją: odtwarzają błąd 1.27.0 co do numeru linii i przepuszczają wersję naprawioną. |
| **D17** | Firmware **jest** teraz kompilowane prawdziwym toolchainem (`tests/kompiluj_firmware.sh`) | W „Czego nie zweryfikowano" od zawsze stało, że firmware nigdy nie przeszło przez kompilator Arduino. Testy sprawdzają wycięte funkcje na zaślepkach — to łapie błędy logiki, ale nie literówkę w nazwie z Arduino API ani funkcję, która w rdzeniu 3.x zmieniła sygnaturę.<br>Skrypt buduje **oba** szkice na `esp32:esp32@3.3.11` i z **ustawieniami płytki wypisanymi w nagłówku `PillBox.ino`** — czyli tak, jak Kuba naprawdę wgrywa. Wynik: `PillBox.ino` **38% flasha** (1,21 MB z 3 MB), `PillBoxTest.ino` 12%.<br>**Kompilacja na domyślnych ustawieniach mierzy nie to urządzenie.** Domyślny podział daje programowi 1,2 MB, a nagłówek szkicu wymaga `Huge APP` z 3 MB — ten sam plik to raz **92%**, raz **38%**. Pierwszy przebieg tego skryptu pokazał 92% i wyglądało to jak alarm o kończącej się pamięci; alarmu nie ma. Skrypt sprawdza teraz, czy nagłówek nadal zapowiada `Huge APP`, i przerywa, gdy się rozjadą. Nazwy opcji potrafią zmylić: `CDCOnBoot=default` to USB CDC **włączone**, `CDCOnBoot=cdc` — wyłączone.<br>**Morał: kompilacja na innych ustawieniach niż te z wgrywania odpowiada na inne pytanie, niż się zadało.**<br>Trzy rzeczy, które trzeba było obejść, bo bez nich się nie da: (1) kompilujemy jako `.cpp`, nie `.ino` — generator prototypów w arduino-cli używa **łatanej** wersji ctags od Arduino, a zwykły `exuberant-ctags` produkuje dla `static void etapTestu()` prototyp z typem `int` i build pada na sprzecznej deklaracji; nasz kod definiuje wszystko w kolejności użycia, więc prototypy są zbędne. (2) `#line` w nagłówku, żeby numery w błędach wskazywały `.ino`. (3) rdzeń 3.x ciągnie `dfu-util` z indeksu Arduino — narzędzie **wyłącznie do wgrywania przez USB** — więc gdy ten indeks jest nieosiągalny, skrypt podstawia atrapę.<br>**Skrypt NIE jest częścią `run_all.sh`**: wymaga sieci i ~500 MB toolchainu. Uruchamiasz go, gdy ruszasz firmware.<br>**Nadal nie dowodzi, że działa na płytce** — dowodzi, że się buduje. |
| **D15** | Testy **wykonują** kod tam, gdzie dotąd czytały jego treść | Cztery obszary miały pokrycie tylko z nazwy:<br>**1. Reguły bazy** — nic nie sprawdzało, czy zapis przejdzie przez `database.rules.json`. Jedyną obroną był skan, czy pięć nazw pól występuje gdziekolwiek w pliku — przechodził nawet wtedy, gdy pole siedziało pod złym węzłem. Teraz `tests/rules_engine.mjs` czyta prawdziwy plik reguł i jest wpięty w atrapę Firebase, więc **wszystkie testy aplikacji sprawdzają przy okazji zgodność z regułami**. Osobny `test_rules.mjs` pilnuje payloadów firmware — lista pól jest wyciągana ze źródła, więc dołożenie pola do `pushEventRecord()` wywala test. Bez tego powtórka **D6** kończy się kodem 400, a po **B3/D13** dawka jest dodatkowo **kasowana** z kolejki.<br>**2. Kolejka zapisów w telefonie** — 13 wyrażeń regularnych po treści `index.html`, pierwsze sprawdzało dosłownie tyle, że `zapiszPewnie()` istnieje. Żadne jej nie uruchomiło, bo atrapa bazy **nie umiała zawieść**. Teraz umie (`__db.tryb = "blad" \| "wisi"`), więc da się odtworzyć Firebase offline z **D1**: obietnica, która nie jest odrzucana, tylko wisi.<br>**3. `planNextSleep()`** — jedyna funkcja decydująca, czy pudełko obudzi się na 20:00, miała wyłącznie audyt tekstowy. Doszła własność zbiorcza po 3456 kombinacjach flag: sen nigdy nie wychodzi zerowy ani dłuższy niż czas do najbliższej dawki.<br>**4. Zgodność pudełko–aplikacja** poza numerem doby: dopasowanie otwarcia do dawki (`matchSlot()` kontra skan w `doReconcile()`) i kontrakt licznika tabletek.<br>Każdy obszar sprawdzony **mutacjami źródła** — test, który niczego nie wykrywa, jest gorszy niż jego brak, bo daje fałszywe poczucie pokrycia. |
| **N1** | `cstdint` w shimie + ścieżki po usunięciu `app/` | Cały zestaw testów nie startował. Szczegóły: kontekst 3.10 |
